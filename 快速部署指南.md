# LiuTech 快速部署指南

## 本地开发

### 构建和启动
```bash
# 构建项目
.\快速打包文件.bat

# 启动服务
docker compose up -d
```

### 访问地址
- Web前端: http://localhost:3000
- Admin后台: http://localhost:3001
- 后端API: http://localhost:8080
- AI服务: http://localhost:8081

## 服务器部署

### 1. 本地构建并导出镜像
```bash
# 构建项目
.\快速打包文件.bat

# 导出镜像（Windows）
.\镜像导出脚本.bat
```

MySQL 镜像使用 `mysql:8.0`，导出文件名为 `mysql-8.0.tar`。

### 2. 上传文件到服务器
上传到 `/opt/liutech/` 目录：
- `docker-images/` 目录下的所有 `.tar` 文件 → `images/`
- `sql/` 目录（包含数据库初始化文件）
  - `sql.sql` - 主数据库初始化文件
  - `ai_chat_tables.sql` - AI数据库初始化文件
- `nginx/` 目录 → `nginx/` （包含Nginx配置）
- `docker-compose.yml` - Docker编排配置
- `.env` - 环境变量配置
- `服务器部署脚本.sh`

**SSL证书配置（生产环境HTTPS）：**
将SSL证书文件放置到服务器的 `/opt/liutech/nginx/` 目录：
```bash
# 在服务器上执行
mkdir -p /opt/liutech/nginx
# 上传你的证书文件
# liuxin.chat_bundle.crt - SSL证书
# liuxin.chat.key - SSL私钥
```

### 3. 服务器执行部署
```bash
cd /opt/liutech
chmod +x 服务器部署脚本.sh
./服务器部署脚本.sh
```

### 4. 配置环境变量
部署完成后，编辑 `.env` 文件配置必要参数：
```bash
# 编辑环境变量
vi /opt/liutech/.env

# JWT密钥 - 后端和AI服务共用此密钥（必需，32位以上随机字符串）
JWT_SECRET=your_strong_jwt_secret_key_min_32_chars

# AI服务API Key（必需）
SPRING_AI_OPENAI_API_KEY=your_siliconflow_api_key

# 重启服务使配置生效
docker compose restart backend ai
```

获取API Key: [SiliconFlow官网](https://www.siliconflow.com/)

### 5. 访问地址

**HTTP（开发环境）：**

| 服务 | 地址 |
|------|------|
| 用户前端 | http://服务器IP:80 |
| 管理后台 | http://服务器IP:81 |
| 后端API | http://服务器IP:80/api |
| AI服务 | http://服务器IP:80/ai |

**HTTPS（生产环境）：**
配置SSL证书后，HTTP会自动重定向到HTTPS：

| 服务 | 地址 |
|------|------|
| 用户前端 | https://服务器IP（或域名） |
| 管理后台 | http://服务器IP:81 |
| 后端API | https://服务器IP/api |
| AI服务 | https://服务器IP/ai |

## 常用命令

```bash
# 查看所有服务状态
docker compose ps

# 查看所有日志
docker compose logs -f

# 查看AI服务日志
docker compose logs -f ai

# 查看后端日志
docker compose logs -f backend

# 重启所有服务
docker compose restart

# 重启AI服务
docker compose restart ai

# 停止所有服务
docker compose down

# 启动所有服务
docker compose up -d
```

## Docker网络说明

部署后服务间通信使用Docker容器名：
- AI服务 → 后端服务: `http://backend:8080`
- AI服务 → MySQL: `mysql:3306`
- 前端 → AI服务: 通过Nginx `/ai/` 代理

## 故障排查

### AI服务返回 406 Not Acceptable
**原因**：Nginx 配置中强制设置了 `Accept: text/event-stream` 头，导致普通 JSON API 请求失败。

**解决方法**：
1. 检查 Nginx 配置文件，确保 `location /ai/` 块中**没有** `proxy_set_header Accept "text/event-stream";` 这一行
2. 重启 Nginx：`docker compose restart nginx`

### AI服务无法连接后端
检查AI服务日志：
```bash
docker compose logs ai
```
确认 `BLOG_API_URL` 配置为 `http://backend:8080`

### HTTPS无法访问
1. 检查证书文件是否存在：
```bash
ls -la /opt/liutech/nginx/liuxin.chat_bundle.crt
ls -la /opt/liutech/nginx/liuxin.chat.key
```
2. 检查证书是否挂载到容器：
```bash
docker exec liutech-nginx ls -la /etc/nginx/ssl/
```
3. 测试 Nginx 配置：
```bash
docker exec liutech-nginx nginx -t
```

### SSE流无响应
1. 检查Nginx配置是否包含SSE相关配置（`proxy_buffering off`, `proxy_read_timeout 3600s`）
2. 检查防火墙是否开放443端口（HTTPS）或80端口（HTTP）
3. 查看AI服务日志中的错误信息

### 数据库连接失败
```bash
# 检查MySQL健康状态
docker compose ps mysql

# 查看MySQL日志
docker compose logs mysql
```
