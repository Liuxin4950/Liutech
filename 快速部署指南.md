# LiuTech 快速部署指南

## 部署方式选择

本项目提供两套部署路径，按服务器网络环境选择：

| 方式 | 路径 | 适用场景 |
|------|------|----------|
| **A（推荐）** | 本地打包镜像 → 导出 tar → 上传服务器 → 容器编排 | 国内服务器、无法访问 DockerHub 或拉取超时 |
| **B（备选）** | GitHub Actions 推送 DockerHub → 服务器 `docker pull` 拉取 | 海外服务器或有稳定 DockerHub 代理 |

国内服务器**不要**用方式 B：服务器执行 `docker pull` 会卡在 DockerHub 拉取阶段。方式 A 全程离线，业务镜像在本地构建好以 `.tar` 文件上传，服务器不再访问任何镜像仓库。

> 唯一的外部镜像依赖是 `mysql:8.0`（基础镜像，不随业务打包）。首次部署前需确保服务器已具备该镜像，见下方「前置条件」。

## 本地开发

### 构建和启动
```bash
# 构建项目
.\快速打包文件.bat

# 启动服务
docker compose up -d
```

### 访问地址
- Web前端: http://localhost:3000
- Admin后台: http://localhost:3001
- 后端API: http://localhost:8080
- AI服务: http://localhost:8081

## 服务器部署（方式 A：本地离线打包）

### 前置条件

服务器需具备以下环境：
- Docker ≥ 20.10.0 + Docker Compose V2（`docker compose` 子命令可用）
- 已存在 `mysql:8.0` 镜像。国内服务器若拉不到，在本地执行后随业务镜像一起上传：
  ```bash
  docker pull mysql:8.0
  docker save mysql:8.0 -o docker-images/mysql.tar
  # 上传后服务器执行：docker load -i images/mysql.tar
  ```
- 开放端口：80、443、81（Admin）；如需远程维护数据库再按安全组白名单开放 3306。

### 1. 本地构建业务镜像
```bash
.\快速打包文件.bat
```
构建 5 个镜像：`liutech-backend`、`liutech-ai`、`liutech-web`、`liutech-admin`、`liutech-nginx`。

### 2. 导出镜像为 tar
```bash
.\镜像导出脚本.bat
```
导出到 `docker-images/` 目录，共 5 个 `.tar` 文件。**MySQL 不随业务导出**，生产数据保留在 `mysql_data` 数据卷，不会被覆盖。

### 3. 上传文件到服务器

上传到 `/opt/liutech/`，目录结构如下：
```
/opt/liutech/
├── images/                      # 5 个业务镜像 tar（首次部署加 mysql.tar）
│   ├── liutech-backend.tar
│   ├── liutech-ai.tar
│   ├── liutech-web.tar
│   ├── liutech-admin.tar
│   └── liutech-nginx.tar
├── sql/sql.sql                  # 主后端 + AI 服务数据库初始化脚本
├── nginx/                       # Nginx 配置（含 SSL 证书）
├── .env                         # 环境变量（可由脚本生成模板，再填写）
└── 服务器部署脚本.sh
```

> `docker-compose.yml` 由 `服务器部署脚本.sh` 内联生成，无需单独上传。如需自定义服务结构，部署后再编辑 `/opt/liutech/docker-compose.yml`。

**SSL 证书配置（生产 HTTPS）：**
```bash
# 在服务器上执行
mkdir -p /opt/liutech/nginx
# 上传证书文件到该目录
# liuxin.chat_bundle.crt - SSL 证书
# liuxin.chat.key         - SSL 私钥
```

### 4. 服务器执行部署
```bash
cd /opt/liutech
chmod +x 服务器部署脚本.sh
./服务器部署脚本.sh
```

脚本依次完成：建立目录结构 → 复制 SQL/Nginx 配置 → 生成 `docker-compose.yml` → 加载 5 个 tar 镜像 → 生成 `.env` 模板（若不存在）→ `docker compose up -d`。

### 5. 配置环境变量

首次部署脚本会生成 `.env` 模板，编辑填入真实值：
```bash
vi /opt/liutech/.env
```

必填项：
```bash
# JWT 密钥 - 后端和 AI 服务共用（32 位以上随机字符串）
JWT_SECRET=your_strong_jwt_secret_key_min_32_chars

# MySQL root 密码
DB_PASSWORD=your_mysql_root_password

# AI 服务 API Key
SPRING_AI_OPENAI_API_KEY=your_siliconflow_api_key

# SiliconFlow 通用 API Key（后端和 AI 能力共用）
SILICONFLOW_API_KEY=your_siliconflow_api_key

# SiliconFlow TTS API Key（可与通用 API Key 相同）
SILICONFLOW_TTS_API_KEY=your_siliconflow_tts_api_key

# AI 服务调用主后端 TTS 的内部令牌，backend 和 ai 必须一致
TTS_PROXY_INTERNAL_TOKEN=your_tts_internal_token

# 生产环境建议使用公网 HTTPS 域名
SERVER_BASE_URL=https://www.liuxin.chat
```

脚本会检测 `.env` 是否仍为示例值（`your_` 前缀），未修改则拒绝启动。修改后重启生效：
```bash
docker compose restart backend ai
```

获取 API Key: [SiliconFlow 官网](https://www.siliconflow.com/)

### 6. 访问地址

**HTTP（开发环境）：**

| 服务 | 地址 |
|------|------|
| 用户前端 | http://服务器IP:80 |
| 管理后台 | http://服务器IP:81 |
| 后端API | http://服务器IP:80/api |
| AI服务 | http://服务器IP:80/ai |

**HTTPS（生产环境）：**
配置 SSL 证书后，HTTP 自动重定向到 HTTPS：

| 服务 | 地址 |
|------|------|
| 用户前端 | https://服务器IP（或域名） |
| 管理后台 | http://服务器IP:81 |
| 后端API | https://服务器IP/api |
| AI服务 | https://服务器IP/ai |

### 7. 生产配置对齐说明

- 本地和云端的 `docker-compose.yml` 应保持同一套服务结构和环境变量引用。
- `SPRING_DATASOURCE_URL` 需包含 `allowPublicKeyRetrieval=true`，避免 MySQL 8 + JDBC 在公钥检索阶段连接失败。
- `.env` 变量名保持一致，敏感值允许按环境区分；本地可用开发数据库密码，生产使用强密码。
- 业务发布时只重建 `backend ai web admin nginx`，**不要**执行 `docker compose down -v`，**不要**删除 `mysql_data` 数据卷。

## 服务器部署（方式 B：DockerHub 拉取，仅海外/有代理）

仅适用于能稳定访问 DockerHub 的服务器。流程依赖 `.github/workflows/deploy.yml` 自动构建推送，配套 `服务器快速部署.sh` 在服务器拉取。

前置：在 GitHub 仓库 Secrets 配置 `DOCKER_HUB_TOKEN`、`SERVER_HOST`、`SERVER_USER`、`SERVER_SSH_KEY`。推送到 `main` 分支即自动触发构建推送 + SSH 部署。

服务器手动拉取：
```bash
cd /opt/liutech
./服务器快速部署.sh
```

> 国内服务器此方式会卡在 `docker pull`，请改用方式 A。

## 后续更新业务镜像（方式 A）

代码变更后重新发布，只更新业务镜像，不影响数据库：

```bash
# 1. 本地重新构建 + 导出
.\快速打包文件.bat
.\镜像导出脚本.bat

# 2. 上传 docker-images/*.tar 到服务器 /opt/liutech/images/

# 3. 服务器加载并重启业务服务
cd /opt/liutech
docker load -i images/liutech-backend.tar
docker load -i images/liutech-ai.tar
docker load -i images/liutech-web.tar
docker load -i images/liutech-admin.tar
docker load -i images/liutech-nginx.tar
docker compose config -q
docker compose up -d --no-deps --force-recreate backend ai web admin nginx
```

MySQL 镜像和 `mysql_data` 数据卷不参与业务更新。

## 常用命令

```bash
# 查看所有服务状态
docker compose ps

# 查看所有日志
docker compose logs -f

# 查看AI服务日志
docker compose logs -f ai

# 查看后端日志
docker compose logs -f backend

# 重启所有服务
docker compose restart

# 重启AI服务
docker compose restart ai

# 停止所有服务
docker compose down

# 启动所有服务
docker compose up -d
```

## Docker 网络说明

部署后服务间通信使用 Docker 容器名：
- AI服务 → 后端服务: `http://backend:8080`
- AI服务 → MySQL: `mysql:3306`
- 前端 → AI服务: 通过 Nginx `/ai/` 代理

## 故障排查

### AI 服务返回 406 Not Acceptable
**原因**：Nginx 配置中强制设置了 `Accept: text/event-stream` 头，导致普通 JSON API 请求失败。

**解决**：
1. 检查 Nginx 配置文件，确保 `location /ai/` 块中**没有** `proxy_set_header Accept "text/event-stream";`
2. 重启 Nginx：`docker compose restart nginx`

### AI 服务无法连接后端
```bash
docker compose logs ai
```
确认 `BLOG_API_URL` 配置为 `http://backend:8080`。

### HTTPS 无法访问
1. 检查证书文件是否存在：
```bash
ls -la /opt/liutech/nginx/liuxin.chat_bundle.crt
ls -la /opt/liutech/nginx/liuxin.chat.key
```
2. 检查证书是否挂载到容器：
```bash
docker exec liutech-nginx ls -la /etc/nginx/ssl/
```
3. 测试 Nginx 配置：
```bash
docker exec liutech-nginx nginx -t
```

### SSE 流无响应
1. 检查 Nginx 配置是否包含 SSE 相关配置（`proxy_buffering off`、`proxy_read_timeout 3600s`）
2. 检查防火墙是否开放 443 端口（HTTPS）或 80 端口（HTTP）
3. 查看 AI 服务日志中的错误信息

### 数据库连接失败
```bash
# 检查 MySQL 健康状态
docker compose ps mysql

# 查看 MySQL 日志
docker compose logs mysql
```

### 服务器拉取 mysql:8.0 失败（国内）
方式 A 不导出 MySQL 镜像，首次部署若服务器没有 `mysql:8.0`，在本地导出后上传：
```bash
# 本地
docker pull mysql:8.0
docker save mysql:8.0 -o docker-images/mysql.tar
# 服务器
docker load -i /opt/liutech/images/mysql.tar
```
