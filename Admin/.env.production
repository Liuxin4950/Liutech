# 生产环境 API 基础地址
# 推荐：使用 '/api' 通过根 Nginx 反向代理到后端，避免跨域与复杂网络解析
VITE_API_BASE_URL=/api

# 如需“直连后端”，改为服务器外网地址：
# 例如：VITE_API_BASE_URL=http://<服务器IP或域名>:8080
# 切记：不要写 'backend:8080'，该主机名仅在 Docker 内部网络生效，
# 浏览器无法解析容器名，会导致请求失败。