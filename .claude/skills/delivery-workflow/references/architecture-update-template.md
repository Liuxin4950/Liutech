# 功能名 架构说明 - YYYY-MM-DD

> 使用说明：项目架构文档用于让下一次 AI 和开发者快速理解本次功能如何改变系统。重大功能 / 高风险变更必须创建；普通功能如有架构、流程、数据流、接口或部署影响也必须创建。无架构影响时只在开发记录中说明，不必创建本文件。

## 0. 文档信息

| 字段 | 内容 |
| --- | --- |
| 功能名 |  |
| 文件名 | 功能名_YYYY-MM-DD.md |
| 创建日期 | YYYY-MM-DD |
| 关联 PRD | `doc/PRD/功能名_YYYY-MM-DD.md` |
| 关联开发记录 | `doc/记录/开发-功能名_YYYY-MM-DD.md` |

## 1. 本次变更概览

- 变更目标：
- 核心能力：
- 影响范围：
- 不影响范围：

## 2. 影响的系统模块

| 模块 | 是否影响 | 变化说明 |
| --- | --- | --- |
| 模块 1 | 是 / 否 |  |
| 模块 2 | 是 / 否 |  |
| 数据库 / 存储 | 是 / 否 |  |
| 部署 / 配置 / CI | 是 / 否 |  |

## 3. 目录与文件变化

| 路径 | 变化 | 职责 |
| --- | --- | --- |
|  | 新增 / 修改 / 删除 |  |

## 4. 核心类、组件与职责变化

- 接口入口：
- 业务服务：
- 数据访问：
- 前端页面 / 客户端：
- 可复用组件：
- API 调用层：
- 状态管理 / 缓存：

## 5. 接口与调用链路变化

```text
用户操作 -> 前端页面/组件 -> API service -> 后端 Controller -> Service -> Mapper/外部服务 -> 返回结果 -> 前端反馈
```

新增或调整的接口：

| 方法 | 路径 | 调用方 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 6. 数据模型与状态流转变化

- 新增字段 / 表：
- 状态枚举：
- 数据创建路径：
- 数据更新路径：
- 数据删除或失效路径：
- 兼容旧数据方式：

## 7. 权限与安全变化

- 登录态：
- 角色权限：
- 数据归属：
- 输入校验：
- 敏感信息：
- 文件 / 富文本 / AI 输出：

## 8. 性能与缓存变化

- 查询路径：
- 索引：
- 分页：
- 缓存：
- 长连接 / 线程池：
- 前端加载：

## 9. 部署与配置变化

- 环境变量：
- Docker：
- Nginx：
- CI/CD：
- 数据库迁移：

## 10. 对总体架构的影响

说明本次变更是否改变模块边界、数据流、接口协议、权限模型或部署拓扑：

- 模块边界：
- 数据流：
- 接口协议：
- 权限模型：
- 部署拓扑：

## 11. 后续演进建议

- 

## 12. 当前架构同步判断

```text
是否需要更新 doc/记录/当前架构.md：是 / 否
判断依据：
需要同步的长期架构内容：
```

## 13. 架构恢复提示

给下一次 AI 的快速入口：

```text
如果继续开发本功能，优先阅读：
1. 本文档
2. 关联 PRD（业务需求 + 实现方案）
3. 关联开发记录
4. 相关代码路径：
```
