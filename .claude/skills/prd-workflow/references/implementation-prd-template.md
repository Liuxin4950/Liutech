# 功能名 实现方案 - YYYY-MM-DD

> 使用说明：实现方案章节必须基于”已确认的业务需求 + 项目架构 + 真实代码库”生成。它回答”当前项目具体怎么做”，但仍不是开发记录。若规划过程中发现需求需要变更，必须停止并回到业务需求共创阶段。本模板追加在 `## 业务需求` 之后，组成完整的合并 PRD。

## 0. 文档信息

| 字段 | 内容 |
| --- | --- |
| 功能名 |  |
| 文件名 | 功能名_YYYY-MM-DD.md |
| 创建日期 | YYYY-MM-DD |
| 当前状态 | 草稿 / 待确认 / 已确认 / 已废弃 |
| 关联业务需求 | `doc/PRD/功能名_YYYY-MM-DD.md`（同文件业务需求章节） |
| 关联开发记录 | `doc/记录/开发-功能名_YYYY-MM-DD.md` |
| 关联架构文档 | `doc/记录/架构-功能名_YYYY-MM-DD.md` 或“无架构影响” |
| 需求确认状态 | 已确认 / 未确认 |

## 1. 需求摘要

简要复述最终确认的需求，不重新发明需求：

- 用户目标：
- 必须实现：
- 本次不做：
- 关键验收标准：

如与业务需求不一致，必须说明原因并回到需求确认。

## 2. 代码库审查摘要

记录本次为了生成实现方案 实际查看过的内容。

### 2.1 已阅读文档

- `doc/PRD/功能名_YYYY-MM-DD.md`
- `doc/记录/...`
- 其他：

### 2.2 已检查代码路径

| 模块 | 路径 | 发现 |
| --- | --- | --- |
| 前端 / 客户端 |  |  |
| 管理端 / 后台 |  |  |
| 后端服务 |  |  |
| 外部服务 / AI 服务 |  |  |
| 数据库 / 存储 |  |  |
| 部署 / 配置 / CI |  |  |

### 2.3 当前系统现状

说明当前项目已经有什么、缺什么、哪些行为会被复用：

- 已有能力：
- 缺失能力：
- 可复用模块：
- 需要谨慎修改的历史逻辑：

## 3. 影响范围

### 3.1 业务影响

- 

### 3.2 前端影响

- 前端 / 客户端：
- 管理端 / 后台：

### 3.3 后端影响

- 后端服务：
- 外部服务 / AI 服务：

### 3.4 数据库影响

- 表结构：
- 索引：
- 迁移脚本：
- 数据修复：

### 3.5 部署与配置影响

- 环境变量：
- Docker：
- Nginx：
- CI/CD：

如无影响，必须写明“无影响，原因：...”。

## 4. 总体实现方案

用一段话说明整体路线，再给出流程图或步骤。

```text
用户操作 -> 前端页面/组件 -> API 服务 -> 后端 Controller -> Service -> Mapper/外部服务 -> 返回结果 -> 前端反馈
```

关键设计取舍：

- 方案 A：
- 方案 B：
- 选择：
- 原因：

## 5. 后端实现方案

### 5.1 API 设计

| 方法 | 路径 | 权限 | 请求 | 响应 | 说明 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 5.2 Controller 层

- 新增/修改文件：
- 职责：
- 参数校验：
- 错误响应：

### 5.3 Service 层

- 新增/修改文件：
- 核心业务流程：
- 事务边界：
- 幂等性：
- 异常处理：

### 5.4 Mapper / 数据访问

- 新增/修改文件：
- SQL 变化：
- 分页与排序：
- N+1 风险：
- 索引需求：

### 5.5 鉴权与权限

- 是否需要登录：
- 角色限制：
- 数据归属校验：
- 越权风险：

## 6. 前端实现方案

### 6.1 页面与路由

- Web：
- Admin：
- 路由变化：

### 6.2 组件与状态

- 新增/修改组件：
- Pinia 状态：
- Composable：
- 表单校验：
- 错误与空状态：

### 6.3 API 调用

- 新增/修改 service：
- 请求参数：
- 响应处理：
- Loading / retry / cancellation：

### 6.4 交互细节

- 桌面端：
- 移动端：
- 失败提示：
- 成功反馈：

## 7. 数据库与迁移方案

如果涉及数据库，必须填写：

- 表结构变更：
- 新增索引：
- 迁移脚本路径：
- 兼容旧数据：
- 回滚 SQL：

如果不涉及，写明：

```text
本次不涉及数据库结构变化，原因：
```

## 8. 接口与数据流

说明关键数据从哪里来、经过哪些层、最终写到哪里或展示在哪里。

| 数据 | 来源 | 处理层 | 存储/输出 | 备注 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

需要特别标注：

- 用户输入是否可信：
- 后端是否二次校验：
- 返回字段是否包含敏感信息：
- 前端是否可能缓存敏感数据：

## 9. 安全影响

至少检查：

- 认证与授权：
- 输入校验：
- XSS：
- SQL 注入：
- 文件上传/下载：
- SSRF：
- CSRF：
- 敏感信息日志：
- Token / Cookie / localStorage 风险：

结论：

```text
安全风险等级：低 / 中 / 高
必须修复项：
可接受风险：
```

## 10. 性能影响

至少检查：

- 列表分页：
- 数据库索引：
- N+1 查询：
- 大对象传输：
- 缓存策略：
- 线程池 / 连接池：
- 前端包体积：
- 首屏加载：

结论：

```text
性能风险等级：低 / 中 / 高
预估瓶颈：
缓解方案：
```

## 11. 测试方案

### 11.1 后端测试

- 单元测试：
- 集成测试：
- 权限测试：
- 异常测试：

### 11.2 前端测试

- 构建 / 类型检查：
- 页面交互：
- 表单校验：
- 空状态 / 错误状态：

### 11.3 回归测试

- 受影响旧功能：
- 必测路径：

### 11.4 验证命令

```bash
# 示例，按项目适配文件和真实技术栈调整
mvn test
npm run build
```

## 12. 开发拆解

开发记录会承接这里的拆解，但实现方案需要先定义推荐顺序。

| 顺序 | 任务 | 文件/模块 | 完成标准 |
| --- | --- | --- | --- |
| 1 |  |  |  |
| 2 |  |  |  |

## 13. 回滚方案

说明如果上线后出问题如何恢复：

- 代码回滚：
- 配置回滚：
- 数据回滚：
- 灰度或开关：
- 用户影响：

## 14. 架构影响判断

说明是否需要创建或更新 `doc/记录/架构-功能名_YYYY-MM-DD.md`。

```text
是否更新项目架构：是 / 否
判断依据：
需要记录的架构变化：
```

重大功能或高风险变更必须更新项目架构。

## 15. 开放问题

| 问题 | 影响 | 建议 | 是否阻塞开发 |
| --- | --- | --- | --- |
|  |  |  |  |

存在阻塞问题时，不应进入编码。

## 16. 实现方案质量检查清单

- [ ] 已确认业务需求是最终版。
- [ ] 已查询真实代码库，不是泛泛而谈。
- [ ] 已列出影响模块和预计修改文件。
- [ ] 已说明 API、数据流、权限和错误处理。
- [ ] 已检查安全风险。
- [ ] 已检查性能风险。
- [ ] 已定义与项目适配文件匹配的测试方案和验证命令。
- [ ] 已定义回滚方案。
- [ ] 已判断是否需要更新项目架构。
- [ ] 未在需求未确认时直接进入编码。

只有以上清单全部满足，才能交给 `delivery-workflow` 开发。
