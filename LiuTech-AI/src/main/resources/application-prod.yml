spring:
  datasource:
    url: jdbc:mysql://mysql:3306/liutech_ai?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    username: ${DB_AI_APP_USERNAME:liutech_ai_app}
    password: ${DB_AI_APP_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

blog:
  api:
    url: ${BLOG_API_URL:http://backend:8080}

liutech:
  internal-token: ${LIUTECH_INTERNAL_TOKEN}

tts:
  cache:
    dir: ${TTS_CACHE_DIR:/app/tts-cache}
