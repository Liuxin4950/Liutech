# 看板娘前端架构总览

> AI 看板娘是 LiuTech 前端的核心交互功能，涉及聊天、TTS 语音、Live2d 模型、会话管理。
> 本文档为模块入口，读完能定位到具体细分文档。

## 模块职责

看板娘负责：用户与 AI 的多轮对话、流式回复渲染、TTS 语音播报与口型同步、Live2d 模型表情动作驱动、会话历史管理。前端在 `Web/`，后端 AI 服务在 `LiuTech-AI/`（聊天/SSE/TTS 合成）。

`MainLayout` 异步加载 `Live2d` 和 `AiChat`。聊天框的外部尺寸与定位由宿主 `.ai-box > .ai-chat` 管理，避免子组件后加载的 `.chat-box` 默认布局覆盖定位；聊天框内部样式仍归 `AiChat` 所有。

## 文件地图

### 状态与编排

| 文件 | 职责 |
| --- | --- |
| [`Web/src/stores/chat.ts`](../../../../Web/src/stores/chat.ts) | 状态中枢：消息、会话、TTS 队列、显示状态（showModel/showChat/isExpanded）、发送消息、SSE 事件分发 |
| [`Web/src/layouts/MainLayout.vue`](../../../../Web/src/layouts/MainLayout.vue) | 布局宿主：挂载 Live2d + AiChat，桥接模型加载状态、引导步骤、TTS 与音乐事件 |
| [`Web/src/composables/useTtsPlayer.ts`](../../../../Web/src/composables/useTtsPlayer.ts) | TTS 播放器：消费音频队列、驱动口型、音乐互斥、桥接 chatStore 与模型就绪状态 |
| [`Web/src/composables/useOnboarding.ts`](../../../../Web/src/composables/useOnboarding.ts) | 首次引导状态机与版本化持久化（welcome → spotlight → model-tip → chat-tip → done） |

### TTS 与表情

| 文件 | 职责 |
| --- | --- |
| [`Web/src/composables/chatTts.ts`](../../../../Web/src/composables/chatTts.ts) | TTS 音频队列 + Avatar Cue 队列管理，包装 useSequencedBuffer |
| [`Web/src/composables/useSequencedBuffer.ts`](../../../../Web/src/composables/useSequencedBuffer.ts) | 通用 seq 保序缓冲区，解决 SSE 事件乱序到达 |

### 组件

| 文件 | 职责 |
| --- | --- |
| [`Web/src/components/Live2d.vue`](../../../../Web/src/components/Live2d.vue) | PIXI 模型渲染、加载占位与失败重试、ready/error 事件、口型同步、表情/动作驱动、resize |
| [`Web/src/components/AiChat.vue`](../../../../Web/src/components/AiChat.vue) | 聊天框主体，组合 Header/Body/Input，用 chatStore 显示状态 |
| [`Web/src/components/AiChatHeader.vue`](../../../../Web/src/components/AiChatHeader.vue) | 头部按钮群（模式/TTS/历史/模型/清空/展开/关闭） |
| [`Web/src/components/AiChatBody.vue`](../../../../Web/src/components/AiChatBody.vue) | 消息列表、Markdown 渲染、文章推荐卡片 |
| [`Web/src/components/AiChatInput.vue`](../../../../Web/src/components/AiChatInput.vue) | 输入框与语音输入；下一步建议已取消，不额外请求模型 |
| [`Web/src/components/MarkdownRenderer.vue`](../../../../Web/src/components/MarkdownRenderer.vue) | Markdown 流式渲染、代码高亮、DOMPurify 净化 |
| [`Web/src/components/OnboardingGuide.vue`](../../../../Web/src/components/OnboardingGuide.vue) | 三步首次引导：AI 入口、Live2d 就绪、聊天输入区 |

### 会话与语音

| 文件 | 职责 |
| --- | --- |
| [`Web/src/composables/useConversationManager.ts`](../../../../Web/src/composables/useConversationManager.ts) | 会话列表加载/删除/重命名、侧边栏开关 |
| [`Web/src/composables/useVoiceRecognition.ts`](../../../../Web/src/composables/useVoiceRecognition.ts) | Web Speech API 语音识别封装 |

### 服务层

| 文件 | 职责 |
| --- | --- |
| [`Web/src/services/aiStream.ts`](../../../../Web/src/services/aiStream.ts) | SSE 流式聊天，fetch + 断线重连（4 次指数退避） |
| [`Web/src/services/conversation.ts`](../../../../Web/src/services/conversation.ts) | 会话 CRUD API |
| [`Web/src/services/aiRuntime.ts`](../../../../Web/src/services/aiRuntime.ts) | 运行时状态（默认模型、TTS 可用性） |

## 数据流概览

```
用户输入
  └─ AiChatInput ─> AiChat ─> chatStore.sendMessage
                                    │
                                    ▼
                        AiStream.streamChat (SSE POST)
                                    │
              ┌─────────────────────┼─────────────────────┐
              ▼                     ▼                     ▼
        onChunk(data)        onEvent              onComplete
        追加流式消息          事件分发              完成流式
                          ┌──────┴──────┐
                          ▼             ▼
                    avatar-cue       audio
                    入 Cue 队列      入 TTS 队列
                          │             │
                          ▼             ▼
              useTtsPlayer watch    useTtsPlayer watch
              avatarCuePendingCount  ttsPendingCount
                          │             │
                          ▼             ▼
              applyNextAvatarCues   playNextTts
              ─> Live2d 表情        ─> Live2d 口型
```

**显示状态**（showModel/showChat/isExpanded）集中在 chatStore，MainLayout 和 AiChat 直接读写，不再经 props/emit 中转。

## 细分文档

| 文档 | 内容 |
| --- | --- |
| [状态与消息流](状态与消息流.md) | chatStore 状态字段、显示状态提升、sendMessage 流程、SSE 事件分发 |
| [TTS与表情](TTS与表情.md) | seq 保序、audio↔cue 绑定、播放循环、音乐互斥、Avatar Cue |
| [Live2d渲染](Live2d渲染.md) | PIXI 生命周期、ticker 控制、口型同步、表情队列、resize |
| [Markdown与会话](Markdown与会话.md) | 流式渲染、代码高亮、会话历史菜单、持久化 |
| [SSE流式与心跳机制](SSE流式与心跳机制.md) | 完整链路、SseEmitter 源码、心跳保活原理、max_tokens/CDN 断流两次事故复盘 |

## 陷阱与约束

- **流式 Markdown 渲染不要用 rAF 节流**：会"一顿一顿"，保持 computed 同步渲染。性能优化从减少单次解析耗时入手
- **Live2d 隐藏时必须停 ticker**：`visible` prop 控制，否则 PIXI 60fps 空转
- **引导第二步必须消费 Live2d 的真实加载状态**：不能用固定延时假定模型已经可交互
- **chatStore 包装 chatTts 是接口适配**，不是假拆分，不要删除
- **ttsCancelCounter 是合理的响应式桥接**，不要改成回调 hook
- **TTS 与音乐共用 lipSync 单实例**：TTS 优先，恢复音乐前校验用户操作版本
