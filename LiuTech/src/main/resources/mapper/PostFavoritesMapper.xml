<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chat.liuxin.liutech.mapper.PostFavoritesMapper">

    <!-- 结果映射 -->
    <resultMap id="PostFavoritesMap" type="chat.liuxin.liutech.model.PostFavorites">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="postId" column="post_id"/>
        <result property="isFavorite" column="is_favorite"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 查询用户对文章的收藏状态 -->
    <select id="selectByUserIdAndPostId" resultMap="PostFavoritesMap">
        SELECT id, user_id, post_id, is_favorite, created_at, updated_at
        FROM post_favorites
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <!-- 插入或更新收藏记录 -->
    <insert id="insertOrUpdate" parameterType="chat.liuxin.liutech.model.PostFavorites">
        INSERT INTO post_favorites (user_id, post_id, is_favorite, created_at, updated_at)
        VALUES (#{userId}, #{postId}, #{isFavorite}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
        is_favorite = #{isFavorite},
        updated_at = NOW()
    </insert>

    <!-- 统计文章的收藏数 -->
    <select id="countFavoritesByPostId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM post_favorites
        WHERE post_id = #{postId} AND is_favorite = 1
    </select>

    <!-- 删除用户对文章的收藏记录 -->
    <delete id="deleteByUserIdAndPostId">
        DELETE FROM post_favorites
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>
    
    <!-- 获取用户对文章的收藏状态 -->
    <select id="getFavoriteStatus" resultType="java.lang.Integer">
        SELECT is_favorite
        FROM post_favorites
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>
    
    <!-- 插入或更新收藏记录 -->
    <insert id="insertOrUpdateFavorite">
        INSERT INTO post_favorites (user_id, post_id, is_favorite, created_at, updated_at)
        VALUES (#{userId}, #{postId}, #{status}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
        is_favorite = #{status},
        updated_at = NOW()
    </insert>
    
    <!-- 根据文章ID删除所有收藏记录 -->
    <delete id="deleteByPostId">
        DELETE FROM post_favorites WHERE post_id = #{postId}
    </delete>

</mapper>