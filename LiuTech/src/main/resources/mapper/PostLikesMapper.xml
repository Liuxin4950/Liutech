<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chat.liuxin.liutech.mapper.PostLikesMapper">

    <!-- 结果映射 -->
    <resultMap id="PostLikesMap" type="chat.liuxin.liutech.model.PostLikes">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="postId" column="post_id"/>
        <result property="isLike" column="is_like"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 查询用户对文章的点赞状态 -->
    <select id="selectByUserIdAndPostId" resultMap="PostLikesMap">
        SELECT id, user_id, post_id, is_like, created_at, updated_at
        FROM post_likes
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <!-- 插入或更新点赞记录 -->
    <insert id="insertOrUpdate" parameterType="chat.liuxin.liutech.model.PostLikes">
        INSERT INTO post_likes (user_id, post_id, is_like, created_at, updated_at)
        VALUES (#{userId}, #{postId}, #{isLike}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
        is_like = #{isLike},
        updated_at = NOW()
    </insert>

    <!-- 统计文章的点赞数 -->
    <select id="countLikesByPostId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM post_likes
        WHERE post_id = #{postId} AND is_like = 1
    </select>

    <!-- 删除用户对文章的点赞记录 -->
    <delete id="deleteByUserIdAndPostId">
        DELETE FROM post_likes
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>
    
    <!-- 获取用户对文章的点赞状态 -->
    <select id="getLikeStatus" resultType="java.lang.Integer">
        SELECT is_like
        FROM post_likes
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>
    
    <!-- 插入或更新点赞记录 -->
    <insert id="insertOrUpdateLike">
        INSERT INTO post_likes (user_id, post_id, is_like, created_at, updated_at)
        VALUES (#{userId}, #{postId}, #{status}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
        is_like = #{status},
        updated_at = NOW()
    </insert>
    
    <!-- 根据文章ID删除所有点赞记录 -->
    <delete id="deleteByPostId">
        DELETE FROM post_likes WHERE post_id = #{postId}
    </delete>

</mapper>