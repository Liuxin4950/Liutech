<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chat.liuxin.liutech.mapper.PointsTransactionMapper">

    <!-- 管理端分页查询积分流水（关联用户名） -->
    <select id="selectTransactionsForAdmin" resultType="chat.liuxin.liutech.resp.PointsTransactionResp">
        SELECT
            pt.id,
            pt.user_id AS userId,
            pt.transaction_type AS transactionType,
            pt.amount,
            pt.balance_after AS balanceAfter,
            pt.source_type AS sourceType,
            pt.source_id AS sourceId,
            pt.description,
            pt.created_at AS createdAt,
            u.username
        FROM points_transactions pt
        LEFT JOIN users u ON pt.user_id = u.id
        <where>
            <if test="userId != null">
                AND pt.user_id = #{userId}
            </if>
            <if test="transactionType != null and transactionType != ''">
                AND pt.transaction_type = #{transactionType}
            </if>
            <if test="startTime != null">
                AND pt.created_at &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND pt.created_at &lt;= #{endTime}
            </if>
        </where>
        ORDER BY pt.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 管理端统计积分流水总数 -->
    <select id="countTransactionsForAdmin" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM points_transactions pt
        <where>
            <if test="userId != null">
                AND pt.user_id = #{userId}
            </if>
            <if test="transactionType != null and transactionType != ''">
                AND pt.transaction_type = #{transactionType}
            </if>
            <if test="startTime != null">
                AND pt.created_at &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND pt.created_at &lt;= #{endTime}
            </if>
        </where>
    </select>

    <!-- 按多个交易类型求和（SQL 聚合） -->
    <select id="sumPointsByTypes" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0)
        FROM points_transactions
        WHERE transaction_type IN
        <foreach collection="types" item="type" open="(" separator="," close=")">
            #{type}
        </foreach>
    </select>

    <!-- 按单个交易类型求和（SQL 聚合） -->
    <select id="sumPointsByType" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0)
        FROM points_transactions
        WHERE transaction_type = #{type}
    </select>

    <!-- 查询所有用户的积分余额总和（SQL 聚合） -->
    <select id="sumTotalUserPoints" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(points), 0)
        FROM users
        WHERE deleted_at IS NULL
    </select>

</mapper>
