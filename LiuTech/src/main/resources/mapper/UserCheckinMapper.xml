<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chat.liuxin.liutech.mapper.UserCheckinMapper">

    <!-- 管理端分页查询签到记录（关联用户名） -->
    <select id="selectCheckinsForAdmin" resultType="chat.liuxin.liutech.resp.UserCheckinResp">
        SELECT
            uc.id,
            uc.user_id AS userId,
            uc.checkin_date AS checkinDate,
            uc.points_earned AS pointsEarned,
            uc.consecutive_days AS consecutiveDays,
            uc.created_at AS createdAt,
            uc.updated_at AS updatedAt,
            u.username
        FROM user_checkins uc
        LEFT JOIN users u ON uc.user_id = u.id
        <where>
            <if test="userId != null">
                AND uc.user_id = #{userId}
            </if>
            <if test="startDate != null">
                AND uc.checkin_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND uc.checkin_date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY uc.checkin_date DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 管理端统计签到记录总数 -->
    <select id="countCheckinsForAdmin" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM user_checkins uc
        <where>
            <if test="userId != null">
                AND uc.user_id = #{userId}
            </if>
            <if test="startDate != null">
                AND uc.checkin_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND uc.checkin_date &lt;= #{endDate}
            </if>
        </where>
    </select>

</mapper>
