<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chat.liuxin.liutech.mapper.TagsMapper">

    <!-- 查询所有标签（包含文章数量） -->
    <select id="selectTagsWithPostCount" resultType="chat.liuxin.liutech.model.Tags">
        SELECT 
            t.id,
            t.name,
            COUNT(DISTINCT p.id) as post_count
        FROM tags t
        LEFT JOIN post_tags pt ON t.id = pt.tag_id
        LEFT JOIN posts p ON pt.post_id = p.id AND p.deleted_at IS NULL
        GROUP BY t.id, t.name
        ORDER BY t.name
    </select>

    <!-- 根据文章ID查询标签列表 -->
    <select id="selectTagsByPostId" resultType="chat.liuxin.liutech.model.Tags">
        SELECT 
            t.id,
            t.name
        FROM tags t
        INNER JOIN post_tags pt ON t.id = pt.tag_id
        WHERE pt.post_id = #{postId}
        ORDER BY t.name
    </select>

    <!-- 查询热门标签 -->
    <select id="selectHotTags" resultType="chat.liuxin.liutech.model.Tags">
        SELECT 
            t.id,
            t.name,
            COUNT(DISTINCT p.id) as post_count
        FROM tags t
        INNER JOIN post_tags pt ON t.id = pt.tag_id
        INNER JOIN posts p ON pt.post_id = p.id AND p.deleted_at IS NULL
        GROUP BY t.id, t.name
        ORDER BY post_count DESC, t.name
        LIMIT #{limit}
    </select>

    <!-- 根据ID查询标签详情（包含文章数量） -->
    <select id="selectTagByIdWithPostCount" resultType="chat.liuxin.liutech.model.Tags">
        SELECT 
            t.id,
            t.name,
            COUNT(DISTINCT p.id) as post_count
        FROM tags t
        LEFT JOIN post_tags pt ON t.id = pt.tag_id
        LEFT JOIN posts p ON pt.post_id = p.id AND p.deleted_at IS NULL AND p.status = 'published'
        WHERE t.id = #{id}
        GROUP BY t.id, t.name
    </select>

    <!-- 根据标签名字搜索标签（包含文章数量） -->
    <select id="selectTagsByName" resultType="chat.liuxin.liutech.model.Tags">
        SELECT 
            t.id,
            t.name,
            COUNT(DISTINCT p.id) as post_count
        FROM tags t
        LEFT JOIN post_tags pt ON t.id = pt.tag_id
        LEFT JOIN posts p ON pt.post_id = p.id AND p.deleted_at IS NULL
        WHERE t.name LIKE CONCAT('%', #{name}, '%')
        GROUP BY t.id, t.name
        ORDER BY t.name
    </select>

    <!-- 管理端分页查询标签列表 -->
    <select id="selectTagsForAdmin" resultType="chat.liuxin.liutech.resl.TagResl">
        SELECT 
            t.id,
            t.name,
            t.description,
            t.created_at,
            t.updated_at,
            COUNT(DISTINCT p.id) as post_count
        FROM tags t
        LEFT JOIN post_tags pt ON t.id = pt.tag_id
        LEFT JOIN posts p ON pt.post_id = p.id AND p.deleted_at IS NULL
        <where>
            <if test="name != null and name != ''">
                AND t.name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        GROUP BY t.id, t.name, t.description, t.created_at, t.updated_at
        ORDER BY t.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 管理端查询标签总数 -->
    <select id="countTagsForAdmin" resultType="java.lang.Integer">
        SELECT COUNT(DISTINCT t.id)
        FROM tags t
        <where>
            <if test="name != null and name != ''">
                AND t.name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
    </select>

</mapper>