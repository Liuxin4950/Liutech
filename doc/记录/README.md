# 记录目录

本目录只保留长期有效的当前架构文档；历史开发记录与单次架构文档归档在 `archive/`，仅作溯源。

```text
doc/记录/当前架构.md     # 长期有效的总体架构，重大架构变化时更新
doc/记录/archive/         # 历史开发记录与单次架构文档，不再维护
```

过程产物的「改了什么 / 为什么 / 怎么验证」由 git commit message 承担，不再单独写开发记录文件。提交规范见 `CLAUDE.md`。
