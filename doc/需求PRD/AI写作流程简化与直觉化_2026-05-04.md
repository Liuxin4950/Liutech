# AI写作流程简化与直觉化 需求PRD - 2026-05-04

> 使用说明：需求 PRD 只描述"用户要什么、边界是什么、怎样算完成"，不写未经代码库验证的实现方案。

## 0. 文档信息

| 字段 | 内容 |
| --- | --- |
| 功能名 | AI写作流程简化与直觉化 |
| 文件名 | AI写作流程简化与直觉化_2026-05-04.md |
| 创建日期 | 2026-05-04 |
| 当前状态 | 草稿 |
| 需求来源 | 用户提出 — 基于联调体验对现有写作助手流程的反思 |
| 变更级别 | 重大功能 / 高风险变更（涉及 AI SSE 流程重构） |
| 关联实现 PRD | `doc/实现PRD/AI写作流程简化与直觉化_2026-05-04.md` |
| 关联开发记录 | `doc/开发记录/AI文章写作助手与Admin工具隔离_2026-05-04.md` |
| 关联架构文档 | `doc/项目架构/AI文章写作助手与Admin工具隔离_2026-05-04.md` |

## 1. 背景

当前 AI 写作助手存在以下问题：

- **流程过于复杂**：用户输入 → AI 生成草稿 payload → 前端展示草稿卡片 → 用户点"应用全部" → 才写入表单。一个写作动作需要 5 步。
- **接口分裂**：管理员写作助手（`/ai/admin/agent/stream`）和普通 AI 聊天（`/ai/agent/stream`）是两套独立接口，代码重复、维护成本高。
- **AI 不够自主**：AI 生成内容后还需要用户手动"应用"，而不是直接写入对应字段。
- **意图分类多余**：管理员写作助手的所有请求都被分类为各种意图（CHAT、WRITE_ARTICLE 等），但管理员在写作助手里的操作本质上都是写作相关，不需要分类。
- **回退机制缺失**：用户"应用"后如果不满意，没有简单的方式回退某个字段。

用户的核心诉求：**AI 应该像一个真实的助手一样，直接帮你把内容写好，不满意再说。**

## 2. 用户目标

```text
作为博客管理员，我希望在编辑文章时告诉 AI 我想写什么，AI 直接把标题、摘要、正文、分类、标签写入对应字段，
以便我不需要手动操作"应用"步骤，写作过程更流畅。
```

```text
作为博客管理员，如果 AI 写的某个字段不满意，我希望能一键回退该字段到修改前的状态，
以便我不需要重新输入或手动撤销。
```

## 3. 用户场景

| 场景 | 触发条件 | 用户行为 | 期望结果 |
| --- | --- | --- | --- |
| 新建文章 | 管理员在创建文章页输入"帮我写一篇 Docker 入门" | AI 直接生成标题、摘要、正文、分类、标签并写入表单 | 表单字段已填充，用户可直接预览或发布 |
| 丰富内容 | 管理员已有部分内容，在输入框输入"丰富一下正文" | AI 基于当前正文生成更完整的版本，直接替换正文字段 | 正文已更新，其他字段不变 |
| 改写摘要 | 管理员在输入框输入"摘要太长了，精简一下" | AI 基于当前正文生成精简摘要，直接写入摘要字段 | 摘要已更新 |
| 回退操作 | 管理员对 AI 生成的摘要不满意 | 点击摘要字段旁的回退按钮 | 摘要恢复到 AI 修改前的状态 |
| 普通聊天 | 普通用户在看板娘聊天"这篇文章讲了啥" | AI 调用 MCP 工具读取文章并回答 | 正常聊天回复，不影响任何表单 |

## 4. 功能范围

### 4.1 必须实现

- 管理员在文章编辑页的输入框发送消息后，AI 直接将生成的内容写入表单对应字段（标题、摘要、正文、分类、标签）。
- 每次 AI 修改字段前，保存该字段的旧值，用户可通过回退按钮恢复。
- AI 写入内容前经过 DOMPurify 消毒（正文 HTML）。
- 保留执行过程可视化（步骤进度、工具调用状态）。

### 4.2 可选增强

- 输入框右侧或底部悬浮操作栏显示"回退全部"按钮。
- AI 生成过程中实时显示正在写入的字段（如"正在生成正文..."）。

### 4.3 本次不做

- 不合并普通 AI 聊天和写作助手的前端 UI（它们在不同页面，交互不同）。
- 不改变普通用户/游客的聊天体验。
- 不自动保存或自动发布。

## 5. 非目标范围

- 不改变普通用户看板娘聊天的流程和接口。
- 不改变主后端的文章 CRUD 接口。
- 不改变数据库表结构。
- 不做多轮对话上下文管理（每次请求独立）。

## 6. 用户流程

```text
正常流程：
1. 管理员进入文章编辑页（新建或编辑）。
2. 侧边栏显示 AI 写作助手输入框和执行过程区域。
3. 管理员输入写作需求（如"帮我写一篇 Docker 入门"）。
4. AI 生成标题、摘要、正文 HTML、分类、标签。
5. AI 将内容直接写入表单对应字段，侧边栏显示执行完成。
6. 管理员查看结果，满意则保存/发布。
7. 如果某个字段不满意，点击该字段旁的回退图标，恢复到 AI 修改前的值。
```

```text
异常路径 A — AI 生成失败：
1. AI 调用超时或返回错误。
2. 侧边栏显示错误提示。
3. 表单字段保持不变（未被修改）。
```

```text
异常路径 B — 回退操作：
1. 管理员点击某字段的回退按钮。
2. 该字段恢复到 AI 最近一次修改前的值。
3. 回退按钮消失（已恢复到旧值）。
```

## 7. 业务规则

- **权限规则**：只有管理员（ROLE_ADMIN）能使用 AI 写作助手。普通用户看到的是普通聊天，不涉及表单操作。
- **写入规则**：AI 生成的内容直接写入表单，不经过"草稿"中间层。
- **回退规则**：每个字段独立回退，回退后恢复到 AI 修改前的值。多次 AI 修改只保留最近一次的旧值。
- **消毒规则**：AI 生成的正文 HTML 必须经过 DOMPurify 消毒后才能写入表单。
- **失败规则**：AI 生成失败时，表单字段不被修改，用户看到错误提示。
- **分类标签规则**：AI 匹配到已有分类/标签时直接选中；未匹配到时仅作为建议显示，不自动创建。

## 8. 交互与体验要求

- **页面入口**：Admin 文章编辑弹窗右侧侧边栏；Web `/create` 页面右侧侧边栏。
- **操作反馈**：AI 写入字段后，字段区域短暂高亮（如 2 秒绿色边框），提示用户哪些字段被修改了。
- **回退按钮**：仅在字段被 AI 修改后显示，位于字段右上角或右侧，图标为回退箭头。
- **加载状态**：侧边栏显示当前 AI 正在执行的步骤（理解目标 → 读取上下文 → 匹配分类标签 → 生成正文）。
- **错误提示**：AI 失败时在侧边栏显示红色错误信息，表单不受影响。
- **空状态**：未发送过消息时，侧边栏显示快捷提示词（如"帮我写一篇技术博客"）。

## 9. 验收标准

```text
AC1 — AI 直接写入字段：
Given 管理员在文章编辑页，表单为空
When 输入"帮我写一篇 Docker 入门"并发送
Then 表单的标题、摘要、正文、分类、标签字段应在 AI 完成后自动填充
And 不应出现"草稿卡片"或"应用"按钮
```

```text
AC2 — 回退单个字段：
Given AI 已写入标题和正文
When 管理员点击标题字段旁的回退按钮
Then 标题应恢复到 AI 修改前的值
And 正文应保持 AI 生成的值不变
```

```text
AC3 — AI 失败不影响表单：
Given 管理员已有部分内容
When AI 生成超时或失败
Then 表单所有字段应保持不变
And 侧边栏应显示错误提示
```

```text
AC4 — DOMPurify 消毒：
Given AI 生成了包含 <script> 标签的正文
When 内容写入表单
Then <script> 标签应被移除
And 正文应保留安全的 HTML 标签
```

```text
AC5 — 普通聊天不受影响：
Given 普通用户在看板娘聊天
When 发送"这篇文章讲了啥"
Then 应正常调用 MCP 工具并返回聊天回复
And 不应修改任何表单字段
```

## 10. 安全、性能与合规边界

- **安全**：AI 生成的 HTML 必须经过 DOMPurify 消毒。管理员接口仍需 ROLE_ADMIN 权限。
- **性能**：AI 生成耗时取决于模型响应速度，前端应显示加载状态。不阻塞用户其他操作。
- **合规**：不涉及用户隐私数据、支付、第三方登录。

## 11. 约束与假设

- 假设 1：前端表单字段可以通过编程方式直接赋值（Vue 响应式数据绑定）。
- 假设 2：分类和标签的匹配结果可以直接通过 categoryId/tagIds 选中表单中的选项。
- 假设 3：普通聊天和写作助手虽然共用后端 AI 服务，但前端在不同页面，不会互相干扰。

## 12. 待确认问题

| 问题 | 影响 | 建议选项 | 用户结论 |
| --- | --- | --- | --- |
| 后端是否合并为一个接口？ | 影响后端架构和代码量 | A) 合并为一个接口，通过请求参数区分写作/聊天 B) 保持两个接口，但写作接口简化 | **已确认：合并为一个接口** |
| 回退 UI 放在哪里？ | 影响前端交互设计 | A) 字段右上角小图标 B) 底部悬浮操作栏统一回退 | **已确认：底部悬浮操作栏** |
| AI 是否应该自动判断要修改哪些字段？ | 影响提示词设计 | A) AI 自主判断 B) 用户在输入框选择要修改的字段 | **已确认：AI 自主判断** |

## 13. 需求审查记录

> 本节由 AI 在读取项目架构和代码库后补充。

### 13.1 与当前代码一致的部分

- 管理员写作助手已在 Admin 侧边栏和 Web `/create` 页面存在。
- DOMPurify 消毒已在 Admin 和 Web 应用层实现。
- SSE 事件流（agent-plan、tool-start、tool-result、writing-draft、complete）已定义。
- 管理员权限校验已通过 `/ai/admin/**` 安全规则实现。

### 13.2 与当前代码或架构冲突的部分

- 当前 `WritingDraftPayload` 是一个独立的草稿结构，需要改为直接写入表单的字段更新指令。
- 当前前端有"应用全部"、"标题"、"摘要"等应用按钮，需要全部移除。
- 当前后端有 `executeAdminStream` 和 `executeStream` 两个入口，需要决定是否合并。
- 当前意图分类器在管理员入口已被强制为 WRITE_ARTICLE，但普通入口仍使用分类器。

### 13.3 安全与性能风险

- AI 直接写入表单意味着 AI 的输出会立即成为用户数据，DOMPurify 消毒更加关键。
- 如果 AI 生成的内容有问题（如注入），用户可能在未察觉的情况下保存/发布。

### 13.4 需要用户决策的问题

- 后端接口是否合并？合并可以减少代码重复，但需要处理普通聊天和写作的不同行为。
- 回退机制的 UI 设计：字段级回退 vs 全局回退。

### 13.5 调整结论

- 保持原需求：AI 直接写入字段、回退机制、DOMPurify 消毒。
- 待确认：后端接口合并策略、回退 UI 位置。

## 14. 用户确认

```text
确认状态：已确认
确认人：liuxin
确认时间：2026-05-04
确认说明：合并接口、底部回退栏、AI 自主判断。方向确认，进入实现 PRD。
```

## 15. 质量检查清单

- [x] 本文档只写需求和边界，没有写未经验证的技术实现。
- [x] 功能范围和非目标范围清晰。
- [x] 用户流程包含正常路径和必要异常路径。
- [x] 验收标准可测试、可判断。
- [x] 安全、性能、权限、数据边界已说明。
- [x] 待确认问题已列出，且不会被 AI 擅自决策。
- [x] 文件名与后续实现 PRD、开发记录、项目架构保持一致。
