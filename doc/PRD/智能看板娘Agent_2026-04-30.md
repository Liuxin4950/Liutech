# 智能看板娘Agent PRD - 2026-04-30

<!-- 合并自原 doc/PRD/需求-智能看板娘Agent_2026-04-30.md 与 doc/PRD/实现-智能看板娘Agent_2026-04-30.md -->

## 业务需求（来自原需求 PRD）

# 智能看板娘Agent 需求PRD - 2026-04-30

## 0. 文档信息

| 字段 | 内容 |
| --- | --- |
| 功能名 | 智能看板娘Agent |
| 文件名 | 智能看板娘Agent_2026-04-30.md |
| 创建日期 | 2026-04-30 |
| 当前状态 | 已确认 |
| 需求来源 | 用户提出 |
| 变更级别 | 重大功能 / 高风险变更 |
| 关联实现 PRD | `doc/PRD/智能看板娘Agent_2026-04-30.md` |
| 关联开发记录 | `doc/记录/开发-智能看板娘Agent_2026-04-30.md` |
| 关联架构文档 | `doc/记录/架构-智能看板娘Agent_2026-04-30.md` |

## 1. 背景

当前 LiuTech-AI 已具备基础聊天、文章上下文注入、文章搜索推荐、会话记忆、SSE 流式输出和 TTS 能力，但整体更接近“一次问答式 AI 助手”。用户希望把它升级为博客中的智能看板娘，既能陪普通访客聊天、理解文章、推荐和搜索内容，也能在识别管理员身份后承担写作辅助、草稿生成、文章发布、上下架管理等复杂任务。

现有体验的主要不足：

- 一次对话只能生成回答，缺少“思考、计划、调用工具、执行、回看结果”的 Agent 流程。
- 普通用户能力和管理员能力尚未形成清晰的权限隔离。
- 文章查询、推荐、总结、写作、发布和管理没有统一的任务状态和执行反馈。
- AI 服务目录与职责已有膨胀趋势，后续继续加 Agent 能力会影响维护性和性能。
- 当前数据库脚本和实体存在版本漂移风险，需要在升级前统一数据模型边界。

## 2. 用户目标

作为普通访客，我希望博客看板娘能理解我当前所在页面和阅读内容，帮助我总结文章、解释概念、搜索文章、推荐相关内容，并进行自然聊天，以便更快找到和理解博客内容。

作为登录用户，我希望看板娘能记住同一会话上下文，在后续追问中延续上下文，以便获得更连贯的辅助体验。

作为管理员，我希望看板娘在确认我具备管理员权限后，可以辅助写博客、润色内容、生成标题摘要标签、创建草稿、发布文章、管理文章上下架，并在高风险操作前向我确认，以便提高内容生产和管理效率。

## 3. 用户场景

| 场景 | 触发条件 | 用户行为 | 期望结果 |
| --- | --- | --- | --- |
| 文章总结 | 用户在文章详情页 | 询问“这篇文章讲了什么” | 看板娘基于当前文章内容总结，不凭空编造 |
| 概念解释 | 用户阅读技术文章 | 询问文章中的概念或代码 | 看板娘结合文章上下文解释，并指出与文章相关的位置或主题 |
| 文章搜索 | 用户想找内容 | 输入“找一下 JWT 相关的文章” | 看板娘调用真实文章搜索能力，返回匹配文章和简短推荐理由 |
| 个性推荐 | 用户想继续阅读 | 输入“推荐几篇类似文章” | 看板娘基于当前文章、最近推荐结果或关键词推荐真实文章 |
| 日常聊天 | 用户无明确站内任务 | 与看板娘聊天 | 看板娘保持站内第一人称、温和自然的角色气质 |
| 写作辅助 | 管理员在编辑器或 AI 面板 | 输入主题、提纲或草稿 | 看板娘生成大纲、标题、摘要、标签建议或文章正文 |
| 创建草稿 | 管理员要求“帮我写一篇并保存草稿” | 提供主题和要求 | 看板娘先生成内容预览，管理员确认后创建草稿 |
| 发布文章 | 管理员要求发布 | 确认文章内容、分类、标签、状态 | Agent 首次生成内容必须先创建草稿；管理员审查或明确同意后，Agent 才能发布正文，并返回文章链接或管理入口 |
| 上下架管理 | 管理员要求管理文章状态 | 指定文章或搜索条件 | 看板娘查询文章，要求确认后执行上下架 |
| 多步任务 | 用户提出复杂目标 | 例如“帮我找旧文并写一篇总结” | 看板娘拆解任务，展示计划，逐步调用工具并汇报进度 |

## 4. 功能范围

### 4.1 必须实现

- 保留并优化现有普通聊天、流式聊天、会话管理、TTS 开关、文章上下文注入能力。
- 将 AI 模块整理为清晰的领域目录，避免聊天、TTS、模型选择、工具执行继续堆在一个服务类中。
- 统一 AI 数据库脚本与实体模型，明确会话、消息、模型配置、Agent 任务和工具调用记录的边界。
- 提供普通用户可用的看板娘能力：
  - 当前文章总结。
  - 当前文章问答。
  - 文章搜索。
  - 文章推荐。
  - 分类、标签、作者和站点信息问答。
  - 普通聊天。
- 提供管理员专属能力：
  - 写作选题、提纲、标题、摘要、标签建议。
  - 文章草稿生成和编辑辅助。
  - 创建草稿。
  - 发布文章。
  - 文章上下架。
  - 查询和管理文章状态。
- Agent 生成的新文章首次必须保存为草稿；管理员审查或明确同意发布后，Agent 才能将草稿发布为正文。
- 本期不开放文章删除能力。
- 实现 Agent 执行模式：
  - 识别用户意图。
  - 判断权限和可用工具。
  - 生成任务计划。
  - 调用查询或写入工具。
  - 对高风险写操作进行确认。
  - 返回执行结果和失败原因。
- 对普通用户和管理员使用不同工具集，普通用户不能看到或调用写入类管理工具。
- 管理员写操作必须具备审计记录，至少记录发起人、任务类型、目标资源、执行结果和时间。
- 性能优化必须覆盖：
  - 常用站点信息、分类、标签和热门/最新文章缓存。
  - 模型调用超时和降级提示。
  - SSE 心跳与任务进度事件。
  - Agent 工具调用次数和最大步骤限制。

### 4.2 可选增强

- 长期记忆摘要，用于跨会话记住管理员写作偏好。
- 文章质量检查，包括错别字、SEO、结构、摘要、标签完整性检查。
- 定时任务或发布计划辅助。
- 多模型路由，例如总结走快速模型，写作走质量模型。
- RAG/向量检索，用于更精准的全站文章语义搜索。
- 看板娘前端动画状态与 Agent 执行状态联动。

### 4.3 本次不做

- 不接入外部搜索引擎作为默认事实来源。
- 不让 AI 绕过主后端权限和业务校验直接写数据库。
- 不支持普通用户执行任何文章写入、发布、上下架或删除操作。
- 不默认自动发布 AI 生成内容；首次生成必须为草稿，发布前必须由管理员审查或明确同意。
- 不在未确认范围内重做整站 UI。

## 5. 非目标范围

- 不改变主博客的核心文章数据归属规则。
- 不改变现有 JWT 登录体系和管理员角色定义。
- 不替代 Admin 后台，AI Agent 是辅助入口，不是唯一管理入口。
- 不承诺 AI 输出完全正确，所有写入操作必须保留用户确认与失败回滚路径。
- 不把 TTS 作为 Agent 能力的强依赖，TTS 是可关闭的体验增强。

## 6. 用户流程

普通用户文章问答流程：

```text
1. 用户进入文章详情页。
2. 前端向 AI 请求中携带 page、postId、最近推荐等上下文。
3. 用户询问总结、解释或推荐。
4. 看板娘读取当前文章和必要站内数据。
5. 看板娘以站内第一人称返回答案。
6. 如涉及推荐，返回真实文章列表和推荐理由。
```

管理员写作辅助流程：

```text
1. 管理员登录后进入文章编辑页或 AI 面板。
2. 管理员提出写作需求，例如“帮我写一篇 Spring AI Agent 实战草稿”。
3. 看板娘识别管理员身份，展示任务计划。
4. 看板娘生成标题、摘要、正文、分类和标签建议。
5. 管理员确认是否创建草稿。
6. 确认后看板娘调用主后端文章创建能力。
7. 系统返回草稿 ID 和可编辑入口。
8. 管理员审查草稿后，可明确要求 Agent 发布该草稿。
```

管理员发布或上下架流程：

```text
1. 管理员提出“把某篇文章发布/下架”。
2. 看板娘先查询并展示目标文章，避免误操作。
3. 如果是 Agent 新生成的文章，系统必须确认它已经先保存为草稿。
4. 系统要求管理员明确确认。
5. 管理员确认后执行状态变更。
6. 看板娘返回执行结果；失败时返回原因和可重试建议。
```

复杂 Agent 任务流程：

```text
1. 用户提出多步目标。
2. 看板娘判断任务类型、权限、所需工具和风险等级。
3. 看板娘输出简短计划。
4. 对只读工具直接执行；对写入工具先停在确认点。
5. 工具执行过程通过 SSE 返回 progress/tool/result/error 事件。
6. 看板娘汇总结果，并保存任务轨迹。
```

异常路径：

```text
1. 当用户未登录或权限不足时，看板娘只提供公开只读能力。
2. 当工具调用失败时，看板娘返回具体失败阶段，不假装执行成功。
3. 当目标文章不唯一时，看板娘要求用户选择具体文章。
4. 当模型输出缺少必要字段时，看板娘应要求补充或重新生成，不直接写入。
```

## 7. 业务规则

- 权限规则：
  - 游客可聊天、总结公开文章、搜索公开文章、查看公开推荐。
  - 登录普通用户可拥有会话记忆，但不能执行管理工具。
  - 管理员可使用写作辅助和文章管理工具。
  - 管理员身份必须以主后端 `/user/current` 的当前角色校验结果为准。
- 工具规则：
  - 普通工具只允许读公开内容。
  - 管理工具必须通过主后端 API 执行，不允许 AI 服务直写主库。
  - 写入类工具必须有确认步骤。
  - Agent 新生成文章的首次写入只能创建草稿。
  - 草稿发布必须由管理员审查或明确同意后执行。
  - 删除类能力本期不开放。
- Agent 规则：
  - 每个 Agent 任务必须有最大步骤数。
  - 每个任务必须有可见状态：规划中、等待确认、执行中、完成、失败、取消。
  - Agent 不能把模型猜测当作工具结果。
- 内容规则：
  - AI 生成文章必须先作为草稿。
  - 发布前需要管理员审查或明确确认标题、正文、分类、标签和状态。
  - 推荐和搜索结果必须返回结构化数据，包含文章 ID、标题、摘要、状态、跳转路径等字段，保证前端可识别并支持点击跳转。
  - AI 总结文章必须基于真实文章内容。
- 数据规则：
  - 会话消息、Agent 步骤、工具调用结果应能追踪。
  - 高风险操作需要记录审计日志。

## 8. 交互与体验要求

- 页面入口：
  - Web 前台保留看板娘聊天入口。
  - 文章详情页可直接询问当前文章。
  - Admin 后台或编辑器中提供管理员 Agent 入口。
- 操作反馈：
  - 流式输出文本。
  - Agent 多步执行时显示当前步骤和工具调用状态。
  - 写入操作前显示确认卡片。
- 加载状态：
  - 普通聊天显示生成中。
  - Agent 任务显示规划中、执行中、等待确认等状态。
- 空状态：
  - 无相关文章时给出替代搜索建议。
  - 无权限时说明当前只能做只读辅助。
- 错误提示：
  - 区分模型失败、工具失败、权限失败、参数缺失、目标不唯一。
- 移动端要求：
  - 普通看板娘入口需要兼容移动端。
  - 管理员写作能力优先放在 Admin 文章编辑器侧边栏，并优先保证桌面端体验。
- 兼容性要求：
  - 沿用当前项目交互风格，由实现 PRD 结合现有页面确定。

## 9. 验收标准

AC1：文章总结
Given 用户位于已发布文章详情页
When 用户询问“总结这篇文章”
Then 看板娘应基于该文章真实内容返回摘要，不引用不存在的内容。

AC2：文章搜索
Given 用户输入“找一下 JWT 相关文章”
When AI 判断这是搜索意图
Then 系统应调用真实文章搜索能力，并返回文章标题、摘要或推荐理由。

AC3：普通用户权限隔离
Given 当前用户不是管理员
When 用户要求“帮我发布文章”
Then 看板娘应拒绝执行写入操作，并说明需要管理员权限。

AC4：管理员创建草稿
Given 当前用户是管理员
When 管理员要求生成文章草稿并确认创建
Then 系统应创建草稿并返回草稿 ID 或编辑入口。

AC5：管理员发布确认
Given 当前用户是管理员
When 管理员要求发布某篇文章
Then 系统应先展示目标文章和发布动作，确认该文章不是未经审查的新生成内容，等待管理员确认后再执行。

AC6：目标不唯一
Given 管理员要求“下架最新那篇文章”但系统无法唯一定位
When Agent 查询到多个可能目标
Then 系统应要求管理员选择具体文章，不直接执行。

AC7：Agent 进度
Given 用户发起多步任务
When Agent 执行查询、生成、写入等步骤
Then 前端应收到可区分的计划、进度、工具结果、完成或失败事件。

AC8：性能边界
Given 普通文章总结或搜索推荐请求
When 后端和 AI 提供商正常
Then 首个流式响应应尽快返回，工具查询类数据应避免重复远程请求。

AC9：审计记录
Given 管理员执行创建草稿、发布、上下架等写操作
When 操作完成或失败
Then 系统应记录操作者、动作、目标、结果和时间。

AC10：失败处理
Given AI 模型或主后端工具调用失败
When Agent 无法继续执行
Then 看板娘应说明失败阶段和原因，不保存虚假的成功状态。

AC11：结构化推荐和搜索结果
Given 用户要求搜索或推荐文章
When AI 返回推荐或搜索结果
Then 后端响应中应包含前端可识别的结构化文章数据，至少包含 id、title、summary、status、url/path 和 reason，前端可据此渲染可点击跳转项。

## 10. 安全、性能与合规边界

- 涉及登录态、管理员角色、JWT、主后端权限校验。
- 涉及富文本文章内容生成与发布，需遵守现有富文本消毒和展示边界。
- 涉及第三方 AI 接口调用，不能在日志中泄漏 API Key、JWT、用户隐私或完整敏感请求。
- 写入类工具必须走主后端 API，继承主后端权限校验和业务校验。
- AI 服务需要限制 Agent 最大步骤数、工具调用次数、单次输入长度、单次输出长度。
- 对高频只读数据应设置缓存或短 TTL，降低主后端和模型调用压力。
- SSE 需要支持心跳、超时、取消和错误事件。
- 管理员写操作必须审计，便于追踪误操作。

## 11. 约束与假设

- 假设主后端已有或可补充文章创建、更新、发布、下架、查询管理接口。
- 假设 AI 服务继续通过主后端 `/user/current` 校验用户角色。
- 假设普通看板娘和管理员 Agent 可以共用基础聊天 UI，但管理员能力需要额外确认交互。
- 假设本期优先实现工具调用式 Agent，不先引入复杂多 Agent 协作框架。
- 假设发布操作前必须人工确认，不做全自动发布。
- 假设 Admin 文章编辑器侧边栏是本期管理员 Agent 的主要入口。
- 假设推荐和搜索结果以结构化 action/result 字段返回，不能只依赖模型自然语言文本解析。

## 12. 待确认问题

| 问题 | 影响 | 建议选项 | 用户结论 |
| --- | --- | --- | --- |
| 本期管理员是否允许“直接发布”，还是只能创建草稿？ | 决定写入风险和确认流程复杂度 | 推荐先支持创建草稿，发布必须二次确认 | 已确认：允许发布，但 Agent 新生成文章第一次必须先保存草稿；管理员审查或明确同意后才可发布正文 |
| 文章删除是否开放给 Agent？ | 删除风险高，影响审计和恢复 | 推荐本期不做删除，只做上下架 | 已确认：本期不做删除 |
| Agent 是先做规则编排，还是引入 LangGraph/Spring AI Advisors 等更完整 Agent 框架？ | 决定实现复杂度和可维护性 | 推荐先规则编排 + 工具调用 + 状态机，稳定后再升级 | 已确认：本期先实现基本功能，后续慢慢扩展 |
| 是否需要语义搜索/RAG？ | 决定数据库和索引方案 | 推荐二期做，首期使用现有搜索和文章上下文 | 已确认：先实现基本功能，RAG 后续扩展 |
| 管理员写作入口优先放在哪里？ | 决定前端改动范围 | 推荐 Admin 编辑器侧边栏 + Web 看板娘只读入口 | 已确认：放在 Admin 编辑器侧边栏 |
| TTS 是否覆盖 Agent 全流程？ | 影响性能和 SSE 事件设计 | 推荐普通聊天可开启，管理员写作默认关闭 | 待确认 |
| 搜索/推荐结果是否需要结构化响应契约？ | 决定前端能否渲染可点击跳转结果 | 推荐后端返回结构化 result/action，不依赖自然语言解析 | 已确认：必须有前后端约定，方便前端识别和跳转 |

## 13. 需求审查记录

### 13.1 与当前代码一致的部分

- AI 服务已有普通聊天、SSE 流式聊天、会话管理、模型配置、站点/文章上下文、工具查询和 TTS 基础能力。
- 当前架构已明确 AI 服务通过主后端 `/user/current` 校验当前用户状态和角色。
- 主后端已有文章、分类、标签、作者资料等公开查询能力，也存在管理员文章/公告管理能力。
- 当前 Web 前台和 Admin 后台分别承担用户阅读和管理写作入口，适合区分普通看板娘与管理员 Agent。

### 13.2 与当前代码或架构冲突的部分

- AI 模块当前更偏一次问答，没有 Agent 任务、步骤、工具调用审计和确认状态。
- AI 数据库脚本存在多版本漂移，实体字段与模块内 SQL 不完全一致。
- `AiChatServiceImpl` 当前承载聊天、SSE、TTS、模型选择、参数解析等多种职责，继续加入 Agent 会显著增加维护风险。
- 当前工具包命名为 `mcp`，但实际是 Spring AI Tool，并非 MCP Server，长期命名可能误导。

### 13.3 安全与性能风险

- 管理员写入类工具如果缺少确认和审计，可能误发布、误下架文章。
- 如果 Agent 每轮都远程查询主后端和模型，首包延迟和主后端压力会明显升高。
- 如果工具调用结果与模型输出边界不清，AI 可能把猜测内容当作真实执行结果。
- 如果权限判断只依赖前端或 token claim，会造成越权风险；必须以后端当前角色为准。

### 13.4 需要用户决策的问题

- 是否允许 Agent 直接发布文章。
- 是否开放删除能力。
- 首期是否引入完整 Agent 框架，还是先做轻量状态机。
- 是否首期做语义搜索/RAG。
- 管理员写作入口优先放在 Admin 编辑器还是独立 AI 工作台。
- 推荐/搜索结果必须设计结构化响应契约，避免前端解析自然语言。

### 13.5 调整结论

- 保持原需求：看板娘应支持聊天、文章总结、推荐、搜索，管理员应支持写作和文章管理辅助。
- 调整后的需求：所有写入类管理员动作必须先确认；Agent 新生成文章首次必须先创建草稿；管理员审查或明确同意后才能发布；首期做草稿/发布/上下架，不开放删除；推荐/搜索结果必须返回结构化数据契约。
- 暂缓或拆分的内容：语义搜索/RAG、长期记忆、多模型复杂路由、定时发布建议放入二期。

## 14. 用户确认

```text
确认状态：已确认
确认人：用户
确认时间：2026-04-30
确认说明：管理员允许 Agent 发布，但 Agent 新生成文章第一次必须为草稿；管理员审查或明确同意后才可发布正文。本期不做删除，先实现基础能力，后续扩展。管理员入口放 Admin 编辑器侧边栏。搜索/推荐结果必须有前后端结构化约定，支持前端识别和点击跳转。
```

## 15. 质量检查清单

- [x] 本文档只写需求和边界，没有写未经验证的技术实现。
- [x] 功能范围和非目标范围清晰。
- [x] 用户流程包含正常路径和必要异常路径。
- [x] 验收标准可测试、可判断。
- [x] 安全、性能、权限、数据边界已说明。
- [x] 待确认问题已列出，且不会被 AI 擅自决策。
- [x] 文件名与后续实现 PRD、开发记录、项目架构保持一致。


## 实现方案（来自原实现 PRD）

# 智能看板娘Agent 实现PRD - 2026-04-30

## 0. 文档信息

| 字段 | 内容 |
| --- | --- |
| 功能名 | 智能看板娘Agent |
| 文件名 | 智能看板娘Agent_2026-04-30.md |
| 创建日期 | 2026-04-30 |
| 当前状态 | 已确认 / 已实施 |
| 关联需求 PRD | `doc/PRD/智能看板娘Agent_2026-04-30.md` |
| 关联开发记录 | `doc/记录/开发-智能看板娘Agent_2026-04-30.md` |
| 关联架构文档 | `doc/记录/架构-智能看板娘Agent_2026-04-30.md` |
| 需求确认状态 | 已确认 |

## 1. 需求摘要

- 用户目标：把 LiuTech-AI 从一次问答助手升级为博客智能看板娘 Agent，支持文章总结、搜索、推荐、聊天，以及管理员写作、草稿、发布、上下架辅助。
- 必须实现：普通用户只读能力、管理员写作管理能力、轻量 Agent 状态机、结构化推荐/搜索响应契约、Admin 编辑器侧边栏、AI 后端文件架构整理。
- 本次不做：文章删除、RAG/语义搜索、长期记忆、多模型复杂路由、完整多 Agent 框架。
- 关键验收：Agent 新生成文章首次必须创建草稿；管理员审查或明确同意后才可发布；推荐/搜索结果必须返回前端可识别并可跳转的结构化数据。

## 2. 代码库审查摘要

### 2.1 已阅读文档

- `doc/PRD/智能看板娘Agent_2026-04-30.md`
- `doc/记录/当前架构.md`
- `LiuTech-AI/AI接口文档.md`
- `LiuTech/API文档.md`
- `.claude/project-adapter.md`
- `.claude/rules/ai-development-workflow.md`

### 2.2 已检查代码路径

| 模块 | 路径 | 发现 |
| --- | --- | --- |
| AI 服务 | `LiuTech-AI/src/main/java/chat/liuxin/ai` | 当前按通用 MVC 分层，`service` 目录承载过多职责，需要领域化整理 |
| AI 聊天 | `LiuTech-AI/src/main/java/chat/liuxin/ai/service/impl/AiChatServiceImpl.java` | 同时处理聊天、SSE、TTS、模型选择、参数合并，需拆分 |
| AI 推荐 | `LiuTech-AI/src/main/java/chat/liuxin/ai/controller/AiRecommendController.java` | 已有推荐接口，但返回契约需补充跳转字段和结果类型 |
| AI 工具 | `LiuTech-AI/src/main/java/chat/liuxin/ai/mcp` | 实际为 Spring AI Tool，不是 MCP Server，建议重命名为 `tool` |
| 主后端文章管理 | `LiuTech/src/main/java/chat/liuxin/liutech/controller/admin/PostsAdminController.java` | 已有文章创建、更新、状态更新、发布、下线接口，可由 AI 服务通过主后端 API 调用 |
| 主后端文章请求 | `LiuTech/src/main/java/chat/liuxin/liutech/req/PostCreateReq.java`、`PostUpdateReq.java` | 创建/更新字段满足 Agent 草稿与发布基础需求 |
| Admin 文章页 | `Admin/src/views/admin/PostsManagement.vue` | 当前使用弹窗 + TinyMCE 编辑，适合新增右侧 Agent 侧边栏 |
| Admin 文章服务 | `Admin/src/services/posts.ts` | 已有文章创建、详情、状态更新服务，可复用类型和请求习惯 |
| Web AI 流式 | `Web/src/services/aiStream.ts` | 已支持 `onEvent(eventType, payload)`，可扩展结构化事件 |
| AI 数据库脚本 | `sql/sql.sql` | 当前统一权威 SQL 已合并主后端和 AI 服务表结构 |

### 2.3 当前系统现状

- 已有能力：聊天、SSE、TTS、模型配置、会话消息、文章上下文、文章推荐、博客 API client、JWT 回查主后端。
- 缺失能力：Agent 任务状态、工具调用审计、写入工具、管理员确认流、结构化搜索/推荐事件契约、Admin 侧边栏。
- 可复用模块：`BlogApiClient`、`PromptAssembler`、`MemoryService`、`PostsAdminController`、Admin `PostsService`、Web `AiStream` 事件解析。
- 需谨慎修改：`AiChatServiceImpl` 当前是主链路，重构要保持 `/ai/chat` 和 `/ai/chat/stream` 兼容；主后端文章发布接口当前部分 operatorId 写死为 `1L`，实现时需评估是否修正。

## 3. 后端文件架构与命名硬约束

本功能开发必须把 AI 后端结构整理为可长期扩展的领域包。新增代码不得继续堆在 `service`、`dto`、`req`、`resp`、`mcp` 这些泛化目录中。

### 3.1 目标包结构

```text
chat.liuxin.ai
├── agent
│   ├── controller
│   ├── request
│   ├── response
│   ├── application
│   ├── domain
│   ├── tool
│   └── persistence
├── chat
│   ├── controller
│   ├── request
│   ├── response
│   ├── application
│   └── stream
├── memory
│   ├── domain
│   ├── mapper
│   └── service
├── model
│   ├── controller
│   ├── request
│   ├── response
│   └── service
├── blog
│   ├── client
│   ├── dto
│   └── context
├── provider
│   ├── llm
│   └── tts
├── security
├── config
├── common
└── monitor
```

### 3.2 命名规范

- `req` 改为 `request`，`resp` 改为 `response`。
- `mcp` 改为 `tool`；除非真正实现 MCP server，否则不使用 MCP 命名。
- `SiliconFlowChatClient` 移入 `provider.llm`。
- `TtsClient` 移入 `provider.tts`。
- `BlogApiClient` 移入 `blog.client`。
- `BlogContextService` 移入 `blog.context`。
- `AIServiceException` 改为 `AiServiceException`。
- Agent 请求/响应统一以 `Agent` 前缀命名，例如 `AgentChatRequest`、`AgentStreamEvent`、`AgentActionResult`。
- 结构化推荐结果统一命名为 `ArticleActionCard` 或 `ArticleResultItem`，避免前后端各自理解。

### 3.3 兼容策略

- 第一阶段允许保留旧类作为适配层，保证现有接口不破。
- 新功能只写入新包结构。
- 旧包迁移时尽量一次移动一个领域，避免大爆炸式重构。
- 对外接口路径保持 `/ai/chat`、`/ai/chat/stream`、`/ai/recommend` 兼容，同时新增 `/ai/agent/**`。

## 4. 影响范围

### 4.1 业务影响

- 普通用户会看到更稳定的文章总结、搜索、推荐卡片。
- 管理员可在文章编辑时通过侧边栏让 Agent 生成草稿、润色、补摘要标签，并在确认后发布。
- 写入操作引入确认和审计，不允许静默发布或删除。

### 4.2 前端影响

- Web：扩展 AI 流事件类型，支持结构化推荐/搜索卡片渲染。
- Admin：文章编辑弹窗或页面新增 Agent 侧边栏，支持写作建议、插入到表单、保存草稿、发布确认。

### 4.3 后端影响

- LiuTech-AI：新增 Agent 编排、工具、审计、结构化响应；重构 AI 目录。
- LiuTech：原则上复用现有 admin posts API；如 operatorId 写死影响审计，应修正为当前用户。

### 4.4 数据库影响

- 统一 `sql/sql.sql` 为权威脚本。
- AI 服务新增 Agent 任务表和工具调用日志表。
- 可能补充会话消息 metadata 字段，保证结构化事件可追踪。

### 4.5 部署与配置影响

- 新增 Agent 最大步骤数、工具超时、缓存 TTL 配置。
- Docker/Nginx 路由沿用 `/ai/`，无需新增路由类型。
- SSE 超时与心跳配置继续沿用并扩展事件类型。

## 5. 总体实现方案

整体路线：先整理 AI 后端包结构和 SQL 契约，再引入轻量 Agent 状态机。普通只读任务直接执行工具并返回结构化卡片；管理员写入任务先生成计划和预览，首次保存为草稿，发布/上下架必须等待确认。

```text
用户/Admin 输入
-> Web 看板娘或 Admin 编辑器侧边栏
-> LiuTech-AI /ai/agent/stream
-> AgentOrchestrator 判断身份、意图、工具、风险
-> 只读工具或管理员工具调用主后端 API
-> SSE 返回文本、计划、工具结果、推荐卡片、确认请求、完成事件
-> 前端按事件类型渲染文本/卡片/确认按钮/跳转链接
```

关键设计取舍：

- 方案 A：直接把 Agent 加进 `AiChatServiceImpl`。
- 方案 B：新增 `agent` 领域包，旧聊天只做兼容调用。
- 选择：方案 B。
- 原因：用户非常在意后端文件名称和架构规整，且 Agent 会持续扩展，必须从第一版就有清楚边界。

## 6. 后端实现方案

### 6.1 AI 服务 API 设计

| 方法 | 路径 | 权限 | 请求 | 响应 | 说明 |
| --- | --- | --- | --- | --- | --- |
| POST | `/ai/agent/chat` | 游客/用户/管理员 | `AgentChatRequest` | `AgentChatResponse` | 非流式 Agent，适合简单任务 |
| POST | `/ai/agent/stream` | 游客/用户/管理员 | `AgentChatRequest` | SSE | 主入口，返回文本、计划、工具、推荐、确认 |
| POST | `/ai/agent/actions/{actionId}/confirm` | 登录管理员 | `AgentActionConfirmRequest` | `AgentActionResultResponse` | 确认创建草稿、发布、上下架 |
| GET | `/ai/agent/tasks/{taskId}` | 登录用户 | - | `AgentTaskResponse` | 查询 Agent 任务状态 |
| POST | `/ai/recommend` | 公开 | `RecommendRequest` | `ArticleRecommendationResponse` | 保留兼容，增强结构化字段 |

### 6.2 结构化响应契约

SSE 事件类型：

| event | payload | 前端用途 |
| --- | --- | --- |
| `agent-start` | `{ taskId, conversationId, mode, role }` | 初始化任务 |
| `agent-plan` | `{ steps: AgentPlanStep[] }` | 展示执行计划 |
| `data` | `{ content }` | 流式文本 |
| `article-results` | `{ source, query, items: ArticleResultItem[] }` | 渲染可点击文章卡片 |
| `tool-start` | `{ toolName, displayName }` | 展示工具执行中 |
| `tool-result` | `{ toolName, success, summary }` | 展示工具结果 |
| `confirmation-required` | `{ actionId, actionType, title, description, preview, riskLevel }` | 渲染确认卡片 |
| `action-result` | `{ actionId, success, target }` | 展示写操作结果 |
| `complete` | `{ taskId, conversationId }` | 结束 |
| `error` | `{ code, message, stage }` | 错误提示 |

文章卡片字段：

```json
{
  "id": 123,
  "title": "文章标题",
  "summary": "摘要",
  "status": "published",
  "categoryName": "后端开发",
  "tagNames": ["Java", "Spring AI"],
  "createdAt": "2026-04-30T12:00:00",
  "url": "/posts/123",
  "adminUrl": "/admin/posts/123",
  "reason": "与当前文章同属 Spring AI 主题",
  "source": "search"
}
```

### 6.3 Controller 层

新增：

- `agent/controller/AgentChatController.java`
- `agent/controller/AgentActionController.java`

调整：

- `controller/AiRecommendController.java` 后续迁入 `agent` 或 `blog` 相关包，保持旧路径兼容。

职责：

- 只接收请求、获取当前用户、委托 application 层。
- 不在 Controller 拼 prompt、不直接调模型、不直接调主后端。

### 6.4 Application / Service 层

新增：

- `agent/application/AgentOrchestrator.java`
- `agent/application/AgentIntentClassifier.java`
- `agent/application/AgentPlanService.java`
- `agent/application/AgentActionService.java`
- `agent/application/AgentStreamPublisher.java`
- `agent/application/ArticleResultMapper.java`
- `agent/application/AdminArticleAgentService.java`

拆分：

- `AiChatServiceImpl` 中的 TTS 分段移动到 `provider/tts/TtsSegmenter.java`。
- 模型选择移动到 `model/service/ModelResolver.java`。
- 参数合并移动到 `model/service/ModelParameterResolver.java`。
- SSE 发送封装移动到 `chat/stream/SseEventPublisher.java`。

核心流程：

```text
AgentOrchestrator
1. 获取身份：游客/user/admin。
2. 判断意图：chat / summarize / search / recommend / write / createDraft / publish / statusChange。
3. 生成计划：最多 N 步。
4. 选择工具集：public tools 或 admin tools。
5. 执行只读工具或生成写入预览。
6. 写入动作创建 pending action，返回 confirmation-required。
7. 管理员确认后执行 action。
8. 保存任务和工具调用记录。
```

### 6.5 Tool 层

新增只读工具：

- `agent/tool/PublicArticleTool.java`
  - `searchArticles`
  - `getLatestArticles`
  - `getHotArticles`
  - `getArticleDetail`
  - `getCategories`
  - `getTags`

新增管理员工具：

- `agent/tool/AdminArticleTool.java`
  - `createDraft`
  - `updateDraft`
  - `publishPost`
  - `offlinePost`
  - `searchAdminPosts`

禁止工具：

- 删除、彻底删除、批量删除不进入 Agent 工具集。

### 6.6 Blog Client 层

当前 `BlogApiClient` 需要拆分：

- `blog/client/PublicBlogClient.java`
- `blog/client/AdminBlogClient.java`

`AdminBlogClient` 必须透传当前管理员 JWT 调主后端 `/admin/posts/**`，不能使用固定系统 token，不能直连数据库。

### 6.7 数据访问

新增实体与 mapper：

- `agent/domain/AgentTask.java`
- `agent/domain/AgentAction.java`
- `agent/domain/AgentToolCall.java`
- `agent/persistence/AgentTaskMapper.java`
- `agent/persistence/AgentActionMapper.java`
- `agent/persistence/AgentToolCallMapper.java`

状态枚举建议：

- `AgentTaskStatus`: `PLANNING`、`RUNNING`、`WAITING_CONFIRMATION`、`COMPLETED`、`FAILED`、`CANCELLED`
- `AgentActionStatus`: `PENDING`、`CONFIRMED`、`EXECUTED`、`FAILED`、`EXPIRED`
- `AgentActionType`: `CREATE_DRAFT`、`UPDATE_DRAFT`、`PUBLISH_POST`、`OFFLINE_POST`

## 7. 主后端实现方案

优先复用：

- `POST /admin/posts`
- `PUT /admin/posts/{id}`
- `PUT /admin/posts/{id}/status`
- `PUT /admin/posts/{id}/publish`
- `PUT /admin/posts/{id}/offline`
- `GET /admin/posts`
- `GET /admin/posts/{id}`

需检查和可能修正：

- `PostsAdminController` 中 `publishPost`、`offlinePost`、`updatePostStatus` 当前 operatorId 使用 `1L`，建议改为 `userUtils.getCurrentUserId()`，保证 Agent 和人工操作审计一致。
- `PostCreateReq.status` 即使前端传 `published`，Agent 新文章首次创建也必须由 AI 服务强制改为 `draft`。

## 8. 前端实现方案

### 8.1 Web 前台

- 扩展 `Web/src/services/aiStream.ts` 对事件类型的处理。
- 扩展聊天 store，保存 `article-results` 事件。
- AI 聊天组件渲染文章卡片：
  - 点击 `url` 跳转前台文章详情。
  - 显示 title、summary、categoryName、tagNames、reason。

### 8.2 Admin 后台

新增：

- `Admin/src/components/agent/AdminAgentSidebar.vue`
- `Admin/src/services/agent.ts`
- `Admin/src/types/agent.ts`

修改：

- `Admin/src/views/admin/PostsManagement.vue`
  - 在新建/编辑文章弹窗或编辑区域右侧加入 Agent 侧边栏。
  - 侧边栏可读取当前表单 title/content/summary/categoryId/tagIds/status。
  - Agent 返回写作建议时，管理员可点击“插入正文”“替换摘要”“应用标签建议”。
  - Agent 要创建草稿时展示确认卡片；确认后调用 AI action confirm，由 AI 服务调用主后端。
  - Agent 发布已有草稿时必须展示文章预览和确认按钮。

交互约束：

- 侧边栏是工作工具，不做营销式大卡片。
- 使用 Ant Design Vue 的 Drawer/Panel、Button、Tag、List、Skeleton、Alert、Popconfirm。
- 不在正文编辑区上方堆大量说明文字。

### 8.3 Admin API 类型

`AgentStreamEvent` 类型示例：

```ts
export type AgentStreamEvent =
  | { type: 'agent-plan'; payload: AgentPlanPayload }
  | { type: 'article-results'; payload: ArticleResultsPayload }
  | { type: 'confirmation-required'; payload: ConfirmationRequiredPayload }
  | { type: 'action-result'; payload: ActionResultPayload }
  | { type: 'data'; payload: { content: string } }
  | { type: 'error'; payload: AgentErrorPayload }
```

## 9. 数据库与迁移方案

### 9.1 统一 SQL

- 以根目录 `sql/sql.sql` 为当前权威初始化脚本。
- 保证实体与 SQL 一致：`user_id`、`metadata`、`updated_at`、`ai_model_config` 必须一致。

### 9.2 新增表

建议追加到 `sql/ai_agent_tables.sql` 或合并进 AI 初始化脚本：

```sql
CREATE TABLE ai_agent_task (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  user_id VARCHAR(64) NULL,
  conversation_id BIGINT UNSIGNED NULL,
  intent VARCHAR(64) NOT NULL,
  status VARCHAR(32) NOT NULL,
  input LONGTEXT NOT NULL,
  result_summary TEXT NULL,
  error_message TEXT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_user_created (user_id, created_at),
  KEY idx_status_created (status, created_at)
);

CREATE TABLE ai_agent_action (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  task_id BIGINT UNSIGNED NOT NULL,
  user_id VARCHAR(64) NOT NULL,
  action_type VARCHAR(64) NOT NULL,
  status VARCHAR(32) NOT NULL,
  target_type VARCHAR(64) NULL,
  target_id BIGINT UNSIGNED NULL,
  payload JSON NULL,
  result JSON NULL,
  expires_at DATETIME NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_task_id (task_id),
  KEY idx_user_status (user_id, status)
);

CREATE TABLE ai_agent_tool_call (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  task_id BIGINT UNSIGNED NOT NULL,
  tool_name VARCHAR(100) NOT NULL,
  success TINYINT NOT NULL DEFAULT 0,
  input JSON NULL,
  output JSON NULL,
  error_message TEXT NULL,
  duration_ms BIGINT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_task_id (task_id),
  KEY idx_tool_created (tool_name, created_at)
);
```

### 9.3 回滚 SQL

```sql
DROP TABLE IF EXISTS ai_agent_tool_call;
DROP TABLE IF EXISTS ai_agent_action;
DROP TABLE IF EXISTS ai_agent_task;
```

## 10. 接口与数据流

| 数据 | 来源 | 处理层 | 存储/输出 | 备注 |
| --- | --- | --- | --- | --- |
| 用户消息 | Web/Admin | AgentController -> AgentOrchestrator | ai_chat_message / SSE | 输入不可信，需长度限制 |
| 当前文章上下文 | 前端 context + 主后端 API | BlogContextService | Prompt / SSE | 只读 |
| 推荐结果 | PublicBlogClient | ArticleResultMapper | `article-results` | 必须结构化 |
| 管理员写作草稿 | LLM 输出 + 管理员确认 | AdminArticleAgentService | 主后端 posts | 首次强制 draft |
| 发布/下架动作 | 管理员确认 | AgentActionService | 主后端 posts status | 需要审计 |
| 工具调用记录 | Agent tools | AgentToolCallMapper | ai_agent_tool_call | 不记录敏感 token |

## 11. 安全影响

- 认证与授权：管理员工具只允许 `ROLE_ADMIN`，身份以后端回查结果为准。
- 输入校验：限制 message、title、summary、content 长度；校验 postId/categoryId/tagIds。
- XSS：AI 生成富文本最终仍走现有文章展示消毒；Admin 预览不直接信任 HTML。
- SQL 注入：新增 mapper 使用 MyBatis-Plus 参数化查询，不拼接用户输入。
- SSRF：AI 工具只能调用配置好的主后端 base URL，不允许模型提供任意 URL。
- CSRF：继续使用 Bearer Token。
- 敏感日志：不记录 JWT、API Key、完整富文本大内容。

安全风险等级：高。
必须修复项：写操作确认、管理员权限校验、工具白名单、审计记录。
可接受风险：AI 文案质量不稳定，通过草稿和人工确认缓解。

## 12. 性能影响

- 列表分页：文章搜索和管理查询必须分页。
- 数据库索引：Agent 任务按 user/status/created_at 查询需索引。
- N+1：推荐结果尽量由主后端聚合返回，AI 侧不循环查详情。
- 大对象传输：文章正文只在必要时拉取，列表只返回摘要。
- 缓存策略：分类、标签、作者资料、最新/热门文章短 TTL 缓存。
- 线程池：SSE 和 TTS 不应每请求无限创建线程；TTS 默认管理员 Agent 关闭。
- 首包：Agent 先发 `agent-start` 和 `agent-plan`，再执行较慢工具。

性能风险等级：中到高。
预估瓶颈：LLM 调用、主后端文章详情查询、TTS 并发。
缓解方案：短 TTL 缓存、步骤上限、工具超时、管理员默认关闭 TTS。

## 13. 测试方案

### 13.1 AI 服务测试

- `AgentIntentClassifierTest`：搜索、推荐、写作、发布、下架意图。
- `AgentPermissionTest`：普通用户不能调用管理员工具。
- `ArticleResultMapperTest`：推荐/搜索结果包含 id、title、url、reason。
- `AgentActionServiceTest`：新文章首次强制草稿；发布必须 pending -> confirmed -> executed。
- `AdminBlogClientTest`：透传 JWT，失败时返回清晰错误。
- `SseEventPublisherTest`：事件名称和 payload 契约稳定。

### 13.2 主后端测试

- `PostsAdminControllerTest`：发布/下线使用当前用户 ID。
- 文章状态更新权限测试。

### 13.3 前端测试

- Admin 构建通过。
- Agent 侧边栏可显示计划、文本、文章卡片、确认卡片、执行结果。
- Web AI 聊天可渲染 `article-results`，点击跳转正确。

### 13.4 验证命令

```bash
cd LiuTech-AI && mvn test
cd LiuTech && mvn test
cd Admin && npm run build
cd Web && npm run build
docker-compose config
```

## 14. 开发拆解

| 顺序 | 任务 | 文件/模块 | 完成标准 |
| --- | --- | --- | --- |
| 1 | 统一 AI SQL 与命名架构 | `LiuTech-AI`, `sql` | 目录结构和脚本一致 |
| 2 | 拆分聊天核心服务 | `chat`, `model`, `provider`, `blog` | 旧接口测试通过 |
| 3 | 新增 Agent 数据模型 | `agent/domain`, `agent/persistence` | mapper 测试通过 |
| 4 | 实现只读 Agent 工具 | `agent/tool`, `blog/client` | 搜索/推荐结构化返回 |
| 5 | 实现管理员工具和确认流 | `agent/application`, `AdminBlogClient` | 草稿/发布/下架流程可控 |
| 6 | 扩展 SSE 事件契约 | `AgentStreamPublisher`, 前端 `agent.ts` | 前端可识别事件 |
| 7 | Admin 编辑器侧边栏 | `Admin/src/components/agent` | 可插入建议、确认动作 |
| 8 | Web 推荐卡片 | `Web/src/components/AiChat*` | 可点击跳转 |
| 9 | 测试和文档 | 测试、AI接口文档、架构文档 | 验证命令通过 |

## 15. 回滚方案

- 代码回滚：回退 AI 服务 Agent 相关提交，旧 `/ai/chat` 和 `/ai/recommend` 保持可用。
- 配置回滚：关闭 Agent 入口开关，只保留普通聊天。
- 数据回滚：保留 Agent 表不影响旧功能；必要时执行 drop 表回滚 SQL。
- 前端回滚：隐藏 Admin Agent 侧边栏和 Web 文章卡片事件渲染。
- 用户影响：回滚后失去 Agent 写作/管理能力，普通聊天不受影响。

## 16. 架构影响判断

是否更新项目架构：是。
判断依据：新增 Agent 领域、工具调用审计、管理员写入确认流、AI 后端包结构调整。
需要记录的架构变化：AI 服务领域包结构、Agent 数据流、权限边界、结构化 SSE 契约、数据库新增表。

## 17. 开放问题

| 问题 | 影响 | 建议 | 是否阻塞开发 |
| --- | --- | --- | --- |
| TTS 是否覆盖 Agent 全流程 | 影响 SSE 和性能 | 管理员 Agent 默认关闭 TTS，普通聊天保留开关 | 不阻塞 |
| Admin 文章编辑是否继续用弹窗还是升级页面 | 影响侧边栏空间 | 首期在现有弹窗/页面旁增加 Drawer/侧栏，后续再重构编辑页 | 不阻塞 |
| 是否需要 Agent 总开关 | 便于回滚 | 新增配置 `ai.agent.enabled` | 不阻塞 |

## 18. 实现 PRD 质量检查清单

- [x] 已确认需求 PRD 是最终版。
- [x] 已查询真实代码库，不是泛泛而谈。
- [x] 已列出影响模块和预计修改文件。
- [x] 已说明 API、数据流、权限和错误处理。
- [x] 已检查安全风险。
- [x] 已检查性能风险。
- [x] 已定义与项目适配文件匹配的测试方案和验证命令。
- [x] 已定义回滚方案。
- [x] 已判断是否需要更新项目架构。
- [x] 未在需求未确认时直接进入编码。
