# 生产环境 API 基础地址
# 推荐：使用 '/api' 通过根 Nginx 反向代理到后端，避免跨域、配置更简单
VITE_API_BASE_URL=/api

VITE_AI_BASE_URL=/ai

# 如需“直连后端”，可改为服务器外网地址：
# 例如：VITE_API_BASE_URL=http://<服务器IP或域名>:8080
# 注意：不要写 'backend:8080' —— 该主机名只在 Docker 网络内可解析，
# 浏览器在你的电脑或用户的电脑上无法解析这个容器名。