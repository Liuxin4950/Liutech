需要修改文章表like字段，改为第三范式，
新增点赞表，新增收藏表
点赞表设计{
    id:主键,
    user_id:用户id,
    article_id:文章id,
    is_like:是否点赞,1表示点赞，0表示取消点赞,
    create_time:创建时间,
}
收藏表设计{
    id:主键,
    user_id:用户id,
    article_id:文章id,
    is_collect:是否收藏,1表示收藏，0表示取消收藏,
    create_time:创建时间,
}
注意，在修改点赞表的时候注意修改之前的postlistresl响应数据的like字段，将其改为int类型，默认值为0，1表示点赞，0表示取消点赞。
修改mapper的语句精选多表查询，同时前端接口获取响应列表用的ts的接口设置也需要改，同时要注意后端有统一的异常处理，可以在异常处理中返回自定义的异常信息。

新增点赞接口，取消点赞接口，获取点赞列表接口，
新增收藏接口，取消收藏接口，获取收藏列表接口



